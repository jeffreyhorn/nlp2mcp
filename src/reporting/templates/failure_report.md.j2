# GAMSLIB Failure Analysis Report

**Generated:** {{ timestamp }}
**nlp2mcp Version:** {{ environment.nlp2mcp_version }}
**Data Source:** {{ data_source }}

---

## Executive Summary

- **Total Models:** {{ summary.total_models }}
- **Total Failures:** {{ summary.total_failures }} ({{ "%.1f" | format(summary.total_failures / summary.total_models * 100) }}%)
- **Unique Error Types:** {{ summary.unique_error_types }}
- **Dominant Blocker:** `{{ summary.dominant_error }}` ({{ summary.dominant_stage }} stage)

---

## Error Distribution by Stage

{{ stage_overview_table }}

---

## Parse Failures

**Total:** {{ parse_failures }} failures ({{ "%.1f" | format(parse_failure_rate * 100) }}% of attempted)

### Error Breakdown

{{ parse_error_table }}

{% for error in parse_errors %}
### {{ error.name }}

| Metric | Value |
|--------|-------|
| Count | {{ error.count }} |
| % of parse failures | {{ "%.1f" | format(error.percentage) }}% |
| % of all models | {{ "%.1f" | format(error.percentage_of_total) }}% |
| Fixable | {{ "Yes" if error.fixable else "No" }} |
| Effort | {{ error.effort_hours }}h |
| Priority Score | {{ "%.2f" | format(error.priority_score) }} |

---

{% endfor %}

## Translation Failures

**Total:** {{ translate_failures }} failures ({{ "%.1f" | format(translate_failure_rate * 100) }}% of attempted)

### Error Breakdown

{{ translate_error_table }}

{% for error in translate_errors %}
### {{ error.name }}

| Metric | Value |
|--------|-------|
| Count | {{ error.count }} |
| % of translate failures | {{ "%.1f" | format(error.percentage) }}% |
| % of all models | {{ "%.1f" | format(error.percentage_of_total) }}% |
| Fixable | {{ "Yes" if error.fixable else "No" }} |
| Effort | {{ error.effort_hours }}h |
| Priority Score | {{ "%.2f" | format(error.priority_score) }} |

---

{% endfor %}

## Solve Failures

**Total:** {{ solve_failures }} failures ({{ "%.1f" | format(solve_failure_rate * 100) }}% of attempted)

### Error Breakdown

{{ solve_error_table }}

{% for error in solve_errors %}
### {{ error.name }}

| Metric | Value |
|--------|-------|
| Count | {{ error.count }} |
| % of solve failures | {{ "%.1f" | format(error.percentage) }}% |
| % of all models | {{ "%.1f" | format(error.percentage_of_total) }}% |
| Fixable | {{ "Yes" if error.fixable else "No" }} |
| Effort | {{ error.effort_hours }}h |
| Priority Score | {{ "%.2f" | format(error.priority_score) }} |

---

{% endfor %}

## Improvement Roadmap

Based on impact analysis (Priority Score = Models / Effort), prioritized fixes:

{{ roadmap_table }}

{% if comparison %}
---

## Progress vs Previous Sprint

{{ comparison.previous_sprint }} to {{ comparison.current_sprint }}

### Success Rate Changes

{{ progress_table }}

### Error Category Changes

{{ error_changes_table }}

{% if comparison.has_regressions %}
### Regressions Detected

{% for regression in comparison.regressions %}
- **{{ regression.stage }}**: {{ regression.description }}
{% endfor %}
{% else %}
No regressions detected.
{% endif %}
{% endif %}

---

*Report generated by `generate_report.py`*
