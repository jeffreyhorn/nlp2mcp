$onText
Generated by nlp2mcp

This file contains the KKT (Karush-Kuhn-Tucker) conditions
for the original NLP model, transformed into MCP format.

KKT System Components:
  - Stationarity: ∇f + J^T λ + J^T ν - π^L + π^U = 0
  - Complementarity: g(x) ⊥ λ, h(x) = 0, bounds ⊥ π
  - Dual feasibility: λ, π^L, π^U ≥ 0
  - Primal feasibility: g(x) ≤ 0, h(x) = 0, lo ≤ x ≤ up
$offText

* ============================================
* Original Model Declarations
* ============================================

Sets
    c /crude, butane, 'mid-c', 'w-tex', 'sr-gas', 'sr-naphtha', 'sr-dist', 'sr-gas-oil', 'sr-res', 'rf-gas', 'fuel-gas', 'cc-gas', 'cc-gas-oil', 'hydro-res', premium, regular, distillate, 'fuel-oil'/
    cf(c) /premium, regular, distillate, 'fuel-oil', 'fuel-gas'/
    cr(c) /'mid-c', 'w-tex'/
    ci(c) /butane, 'sr-gas', 'sr-naphtha', 'sr-dist', 'sr-gas-oil', 'sr-res', 'rf-gas', 'fuel-gas', 'cc-gas', 'cc-gas-oil', 'hydro-res'/
    cd(ci) /butane/
    p /'a-dist', 'n-reform', 'cc-dist', 'cc-gas-oil', hydro/
    m /'a-still', reformer, 'c-crack', hydro/
    lim /lower, upper/
    bp(cf,ci) /premium.butane, premium.'sr-gas', premium.'rf-gas', premium.'cc-gas', premium.'sr-naphtha', regular.butane, regular.'sr-gas', regular.'rf-gas', regular.'cc-gas', regular.'sr-naphtha', distillate.'sr-dist', distillate.'sr-naphtha', distillate.'sr-gas-oil', distillate.'cc-gas-oil', 'fuel-oil'.'sr-gas-oil', 'fuel-oil'.'sr-res', 'fuel-oil'.'cc-gas-oil', 'fuel-oil'.'hydro-res', 'fuel-gas'.'fuel-gas'/
    q /octane, 'vapor-pr', density, sulfur/
;

Parameters
    a(cr,c,p) /'mid-c'.crude.'a-dist' -1, 'mid-c'.'sr-gas'.'a-dist' 0.236, 'mid-c'.'sr-naphtha'.'a-dist' 0.223, 'mid-c'.'sr-naphtha'.'n-reform' -1, 'mid-c'.'sr-dist'.'a-dist' 0.087, 'mid-c'.'sr-dist'.'cc-dist' -1, 'mid-c'.'sr-gas-oil'.'a-dist' 0.111, 'mid-c'.'sr-gas-oil'.'cc-gas-oil' -1, 'mid-c'.'sr-res'.'a-dist' 0.315, 'mid-c'.'rf-gas'.'n-reform' 0.807, 'mid-c'.'fuel-gas'.'a-dist' 0.029, 'mid-c'.'fuel-gas'.'n-reform' 0.129, 'mid-c'.'fuel-gas'.'cc-dist' 0.3, 'mid-c'.'fuel-gas'.'cc-gas-oil' 0.31, 'mid-c'.'cc-gas'.'cc-dist' 0.59, 'mid-c'.'cc-gas'.'cc-gas-oil' 0.59, 'mid-c'.'cc-gas-oil'.'cc-dist' 0.21, 'mid-c'.'cc-gas-oil'.'cc-gas-oil' 0.22, 'w-tex'.crude.'a-dist' -1, 'w-tex'.'sr-gas'.'a-dist' 0.18, 'w-tex'.'sr-naphtha'.'a-dist' 0.196, 'w-tex'.'sr-naphtha'.'n-reform' -1, 'w-tex'.'sr-dist'.'a-dist' 0.073, 'w-tex'.'sr-dist'.'cc-dist' -1, 'w-tex'.'sr-gas-oil'.'a-dist' 0.091, 'w-tex'.'sr-gas-oil'.'cc-gas-oil' -1, 'w-tex'.'sr-res'.'a-dist' 0.443, 'w-tex'.'sr-res'.hydro -1, 'w-tex'.'rf-gas'.'n-reform' 0.836, 'w-tex'.'fuel-gas'.'a-dist' 0.017, 'w-tex'.'fuel-gas'.'n-reform' 0.099, 'w-tex'.'fuel-gas'.'cc-dist' 0.36, 'w-tex'.'fuel-gas'.'cc-gas-oil' 0.38, 'w-tex'.'cc-gas'.'cc-dist' 0.58, 'w-tex'.'cc-gas'.'cc-gas-oil' 0.6, 'w-tex'.'cc-gas-oil'.'cc-dist' 0.15, 'w-tex'.'cc-gas-oil'.'cc-gas-oil' 0.15, 'w-tex'.'hydro-res'.hydro 0.97/
    b(m,p) /'a-still'.'a-dist' 1, reformer.'n-reform' 1, 'c-crack'.'cc-dist' 1, 'c-crack'.'cc-gas-oil' 1/
    k(m) /'a-still' 100, reformer 20, 'c-crack' 30/
    pd(cd) /butane 6.75/
    pr(cr) /'mid-c' 7.5, 'w-tex' 6.5/
    pf(cf) /premium 10.5, regular 9.1, distillate 7.7, 'fuel-gas' 1.5, 'fuel-oil' 6.65/
    ur(cr) /'mid-c' 200, 'w-tex' 200/
    op(p) /'a-dist' 0.1, 'n-reform' 0.15, 'cc-dist' 0.8, 'cc-gas-oil' 0.08, hydro 0.1/
    qs(lim,cf,q) /lower.premium.octane 90, lower.regular.octane 86, upper.premium.'vapor-pr' 12.7, upper.regular.'vapor-pr' 12.7, upper.distillate.density 306, upper.distillate.sulfur 0.5, upper.'fuel-oil'.density 352, upper.'fuel-oil'.sulfur 3.4/
    at(ci,q) /'sr-gas'.octane 78.5, 'sr-gas'.'vapor-pr' 18.4, 'sr-naphtha'.octane 65, 'sr-naphtha'.'vapor-pr' 6.54, 'rf-gas'.octane 104, 'rf-gas'.'vapor-pr' 2.57, 'cc-gas'.octane 93.7, 'cc-gas'.'vapor-pr' 6.9, butane.octane 91.8, butane.'vapor-pr' 199.2/
    atc(cr,ci,q) /'mid-c'.'sr-naphtha'.density 272, 'mid-c'.'sr-naphtha'.sulfur 0.283, 'mid-c'.'sr-dist'.density 292, 'mid-c'.'sr-dist'.sulfur 0.526, 'mid-c'.'sr-gas-oil'.density 295, 'mid-c'.'sr-gas-oil'.sulfur 0.98, 'mid-c'.'cc-gas-oil'.density 294.4, 'mid-c'.'cc-gas-oil'.sulfur 0.353, 'mid-c'.'sr-res'.density 343, 'mid-c'.'sr-res'.sulfur 4.7, 'w-tex'.'sr-naphtha'.density 272, 'w-tex'.'sr-naphtha'.sulfur 1.48, 'w-tex'.'sr-dist'.density 297.6, 'w-tex'.'sr-dist'.sulfur 2.83, 'w-tex'.'sr-gas-oil'.density 303.3, 'w-tex'.'sr-gas-oil'.sulfur 5.05, 'w-tex'.'sr-res'.density 365, 'w-tex'.'sr-res'.sulfur 11, 'w-tex'.'cc-gas-oil'.density 299.1, 'w-tex'.'cc-gas-oil'.sulfur 1.31, 'w-tex'.'hydro-res'.density 365, 'w-tex'.'hydro-res'.sulfur 6/
;

atc(cr,ci,q)$(at(ci,q)) = at(ci,q);

execError = 0;

* ============================================
* Variables (Primal + Multipliers)
* ============================================

* Primal variables: Original decision variables from the NLP
* Multipliers:
*   ν (nu_*): Free multipliers for equality constraints
*   λ (lam_*): Positive multipliers for inequality constraints
*   π^L (piL_*): Positive multipliers for lower bounds
*   π^U (piU_*): Positive multipliers for upper bounds

Variables
    phi
    phir
    phip
    phiw
    nu_bb(cf)
    nu_arev
    nu_amat
    nu_aoper
;

Positive Variables
    z(cr,p)
    x(cf)
    u(cr)
    ui(cr,ci)
    w(cr,ci,cf)
    lam_mbr(cr)
    lam_mb(cr,ci)
    lam_cc(m)
    lam_lcp(cr)
    lam_qlb(cf,q)
    lam_qub(cf,q)
    piL_z(cr,p)
    piL_x(cf)
    piL_u(cr)
    piL_ui(cr,ci)
    piL_w(cr,ci,cf)
;

* ============================================
* NLP Pre-Solve (warm-start for MCP duals)
* ============================================

$onMultiR
$include "data/gamslib/raw/marco.gms"
$offMulti

* Transfer NLP duals to MCP multiplier initialization
lam_mbr.l(cr) = abs(mbr.m(cr));
lam_mb.l(cr,ci) = abs(mb.m(cr,ci));
lam_cc.l(m) = abs(cc.m(m));
lam_lcp.l(cr) = abs(lcp.m(cr));
nu_bb.l(cf) = bb.m(cf);
lam_qlb.l(cf,q) = abs(qlb.m(cf,q));
lam_qub.l(cf,q) = abs(qub.m(cf,q));
nu_arev.l = arev.m;
nu_amat.l = amat.m;
nu_aoper.l = aoper.m;

* Transfer variable marginals to bound multipliers
piL_z.l(cr,p)$(abs(z.l(cr,p) - z.lo(cr,p)) < 1e-6) = abs(z.m(cr,p));
piL_x.l(cf)$(abs(x.l(cf) - x.lo(cf)) < 1e-6) = abs(x.m(cf));
piL_u.l(cr)$(abs(u.l(cr) - u.lo(cr)) < 1e-6) = abs(u.m(cr));
piL_ui.l(cr,ci)$(abs(ui.l(cr,ci) - ui.lo(cr,ci)) < 1e-6) = abs(ui.m(cr,ci));
piL_w.l(cr,ci,cf)$(abs(w.l(cr,ci,cf) - w.lo(cr,ci,cf)) < 1e-6) = abs(w.m(cr,ci,cf));

* Reset any NA/UNDF warm-start multiplier levels to 0 (#1322)
lam_mbr.l(cr)$(NOT (lam_mbr.l(cr) > -inf and lam_mbr.l(cr) < inf)) = 0;
lam_mb.l(cr,ci)$(NOT (lam_mb.l(cr,ci) > -inf and lam_mb.l(cr,ci) < inf)) = 0;
lam_cc.l(m)$(NOT (lam_cc.l(m) > -inf and lam_cc.l(m) < inf)) = 0;
lam_lcp.l(cr)$(NOT (lam_lcp.l(cr) > -inf and lam_lcp.l(cr) < inf)) = 0;
nu_bb.l(cf)$(NOT (nu_bb.l(cf) > -inf and nu_bb.l(cf) < inf)) = 0;
lam_qlb.l(cf,q)$(NOT (lam_qlb.l(cf,q) > -inf and lam_qlb.l(cf,q) < inf)) = 0;
lam_qub.l(cf,q)$(NOT (lam_qub.l(cf,q) > -inf and lam_qub.l(cf,q) < inf)) = 0;
nu_arev.l$(NOT (nu_arev.l > -inf and nu_arev.l < inf)) = 0;
nu_amat.l$(NOT (nu_amat.l > -inf and nu_amat.l < inf)) = 0;
nu_aoper.l$(NOT (nu_aoper.l > -inf and nu_aoper.l < inf)) = 0;
piL_z.l(cr,p)$(NOT (piL_z.l(cr,p) > -inf and piL_z.l(cr,p) < inf)) = 0;
piL_x.l(cf)$(NOT (piL_x.l(cf) > -inf and piL_x.l(cf) < inf)) = 0;
piL_u.l(cr)$(NOT (piL_u.l(cr) > -inf and piL_u.l(cr) < inf)) = 0;
piL_ui.l(cr,ci)$(NOT (piL_ui.l(cr,ci) > -inf and piL_ui.l(cr,ci) < inf)) = 0;
piL_w.l(cr,ci,cf)$(NOT (piL_w.l(cr,ci,cf) > -inf and piL_w.l(cr,ci,cf) < inf)) = 0;

* ============================================
* Equations
* ============================================

* Stationarity: One equation per primal variable (except objvar)
* Complementarity: Equations for inequalities and bounds
* Equality constraints: Original equality constraints

Equations
    stat_phip
    stat_phir
    stat_phiw
    stat_u(cr)
    stat_ui(cr,ci)
    stat_w(cr,ci,cf)
    stat_x(cf)
    stat_z(cr,p)
    comp_cc(m)
    comp_lcp(cr)
    comp_mb(cr,ci)
    comp_mbr(cr)
    comp_qlb(cf,q)
    comp_qub(cf,q)
    comp_lo_u(cr)
    comp_lo_ui(cr,ci)
    comp_lo_w(cr,ci,cf)
    comp_lo_x(cf)
    comp_lo_z(cr,p)
    amat
    aoper
    aprof
    arev
    bb(cf)
;

* ============================================
* Equation Definitions
* ============================================

$onMultiR
* Stationarity equations
stat_phip.. 1 + nu_amat =E= 0;
stat_phir.. -1 + nu_arev =E= 0;
stat_phiw.. 1 + nu_aoper =E= 0;
stat_u(cr).. ((-1) * pr(cr)) * nu_amat - lam_mbr(cr) + lam_lcp(cr) - piL_u(cr) =E= 0;
stat_ui(cr,ci).. (((-1) * 1$(cd(ci))) * lam_mb(cr,ci) - piL_ui(cr,ci))$(cd(ci)) =E= 0;
stat_w(cr,ci,cf).. (((-1) * 1$(bp(cf,ci))) * nu_bb(cf) + 1$(bp(cf,ci)) * lam_mb(cr,ci) + sum(q, (atc(cr,ci,q) * 1$(bp(cf,ci)) * lam_qub(cf,q))$(qs("upper",cf,q))) + sum(q, (((-1) * (atc(cr,ci,q) * 1$(bp(cf,ci)))) * lam_qlb(cf,q))$(qs("lower",cf,q)))$(sameas(cf, 'premium') or sameas(cf, 'regular')) - piL_w(cr,ci,cf))$(bp(cf,ci)) =E= 0;
stat_x(cf).. nu_bb(cf) + ((-1) * pf(cf)) * nu_arev + sum(q, (((-1) * qs("upper",cf,q)) * lam_qub(cf,q))$(qs("upper",cf,q))) + sum(q, (qs("lower",cf,q) * lam_qlb(cf,q))$(qs("lower",cf,q))) - piL_x(cf) =E= 0;
stat_z(cr,p).. ((-1) * op(p)) * nu_aoper + ((-1) * a(cr,cr,p)) * lam_mbr(cr) + sum(ci, ((-1) * a(cr,cr,p)) * lam_mb(cr,ci)) + sum(m, b(m,p) * lam_cc(m)) - piL_z(cr,p) =E= 0;

* Inequality complementarity equations
comp_cc(m).. ((-1) * (sum(p, b(m,p) * sum(cr, z(cr,p))) - k(m))) =G= 0;
comp_lcp(cr).. ((-1) * (u(cr) - ur(cr))) =G= 0;
comp_mb(cr,ci).. sum(p, a(cr,ci,p) * z(cr,p)) + ui(cr,ci)$(cd(ci)) - sum(cf$(bp(cf,ci)), w(cr,ci,cf)) =G= 0;
comp_mbr(cr).. sum(p, a(cr,"crude",p) * z(cr,p)) + u(cr) =G= 0;
comp_qlb(cf,q)$(qs("lower",cf,q)).. sum((cr,ci)$(bp(cf,ci)), atc(cr,ci,q) * w(cr,ci,cf)) - qs("lower",cf,q) * x(cf) =G= 0;
comp_qub(cf,q)$(qs("upper",cf,q)).. ((-1) * (sum((cr,ci)$(bp(cf,ci)), atc(cr,ci,q) * w(cr,ci,cf)) - qs("upper",cf,q) * x(cf))) =G= 0;

* Lower bound complementarity equations
comp_lo_u(cr).. u(cr) - 0 =G= 0;
comp_lo_ui(cr,ci).. ui(cr,ci) - 0 =G= 0;
comp_lo_w(cr,ci,cf).. w(cr,ci,cf) - 0 =G= 0;
comp_lo_x(cf).. x(cf) - 0 =G= 0;
comp_lo_z(cr,p).. z(cr,p) - 0 =G= 0;

* Original equality equations
bb(cf).. x(cf) =E= sum((cr,ci)$(bp(cf,ci)), w(cr,ci,cf));
arev.. phir =E= sum(cf, pf(cf) * x(cf));
amat.. phip =E= sum(cr, pr(cr) * u(cr)) + sum((cd,cr), pd(cd) * ui(cr,cd));
aoper.. phiw =E= sum(p, op(p) * sum(cr, z(cr,p)));
aprof.. phi =E= phir - phip - phiw;

$offMulti

* ============================================
* Fix inactive variable instances
* ============================================

* Variables whose paired MCP equation is conditioned must be
* fixed for excluded instances to satisfy MCP matching.

ui.fx(cr,ci)$(not (cd(ci))) = 0;
piL_ui.fx(cr,ci)$(not (cd(ci))) = 0;
w.fx(cr,ci,cf)$(not (bp(cf,ci))) = 0;
piL_w.fx(cr,ci,cf)$(not (bp(cf,ci))) = 0;
lam_qlb.fx(cf,q)$(not (qs("lower",cf,q))) = 0;
lam_qub.fx(cf,q)$(not (qs("upper",cf,q))) = 0;

* Fix multipliers for empty inequality instances (no variables)
lam_cc.fx('hydro') = 0;

* ============================================
* Model MCP Declaration
* ============================================

* Each line pairs an equation with a variable:
*   equation.variable
*
* This defines the complementarity problem:
*   equation ⊥ variable
*
* Meaning: equation = 0 if variable > 0
*          equation ≥ 0 if variable = 0

Model mcp_model /
    stat_phip.phip,
    stat_phir.phir,
    stat_phiw.phiw,
    stat_u.u,
    stat_ui.ui,
    stat_w.w,
    stat_x.x,
    stat_z.z,
    comp_cc.lam_cc,
    comp_lcp.lam_lcp,
    comp_mb.lam_mb,
    comp_mbr.lam_mbr,
    comp_qlb.lam_qlb,
    comp_qub.lam_qub,
    amat.nu_amat,
    aoper.nu_aoper,
    aprof.phi,
    arev.nu_arev,
    bb.nu_bb,
    comp_lo_u.piL_u,
    comp_lo_ui.piL_ui,
    comp_lo_w.piL_w,
    comp_lo_x.piL_x,
    comp_lo_z.piL_z
/;

* ============================================
* Solve Statement
* ============================================

Solve mcp_model using MCP;

Scalar nlp2mcp_obj_val;
nlp2mcp_obj_val = phi.l;
Display nlp2mcp_obj_val;