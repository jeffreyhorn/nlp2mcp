$onText
Generated by nlp2mcp

This file contains the KKT (Karush-Kuhn-Tucker) conditions
for the original NLP model, transformed into MCP format.

KKT System Components:
  - Stationarity: ∇f + J^T λ + J^T ν - π^L + π^U = 0
  - Complementarity: g(x) ⊥ λ, h(x) = 0, bounds ⊥ π
  - Dual feasibility: λ, π^L, π^U ≥ 0
  - Primal feasibility: g(x) ≤ 0, h(x) = 0, lo ≤ x ≤ up
$offText

* ============================================
* Original Model Declarations
* ============================================

Sets
    tl /'0', '1', '2', '3'/
    t(tl) /'1', '2', '3'/
    tt(t) /'3'/
;

Parameters
    ufact(t)
;

Scalars
    p /1/
    r /0.07/
    tr /0/
    th /1004.72366/
    w /1/
    d /0.2/
    a1 /0.5/
    a2 /-0.5/
    am /1100/
    lambda /0.944/
    lstar /400/
    gamma1 /0.255905/
    gamma2 /1/
;

ufact(t) = lambda ** (ord(t) - 1);

execError = 0;

* ============================================
* Variables (Primal + Multipliers)
* ============================================

* Primal variables: Original decision variables from the NLP
* Multipliers:
*   ν (nu_*): Free multipliers for equality constraints
*   λ (lam_*): Positive multipliers for inequality constraints
*   π^L (piL_*): Positive multipliers for lower bounds
*   π^U (piU_*): Positive multipliers for upper bounds

Variables
    a(tl)
    c(t)
    l(t)
    m(tl)
    n(t)
    obj
    s(tl)
    tax(t)
    u(t)
    y(t)
    nu_utility(t)
    nu_income(tl)
    nu_taxes(t)
    nu_savings(tl)
    nu_budget(tl)
    nu_timemoney(tl)
    nu_terminal(t)
    nu_a_fx_0
    nu_m_fx_0
;

Positive Variables
    lam_dom1(tl)
    lam_dom2(t)
    piL_c(t)
    piL_l(t)
    piL_u(t)
    piU_l(t)
;

* ============================================
* NLP Pre-Solve (warm-start for MCP duals)
* ============================================

$onMultiR
$include "data/gamslib/raw/hhfair.gms"
$offMulti

* Transfer NLP duals to MCP multiplier initialization
nu_utility.l(t) = utility.m(t);
nu_income.l(t) = income.m(t);
nu_taxes.l(t) = taxes.m(t);
nu_savings.l(t) = savings.m(t);
nu_budget.l(tl) = budget.m(tl);
nu_timemoney.l(t) = timemoney.m(t);
nu_terminal.l(tt) = terminal.m(tt);
lam_dom1.l(t) = abs(dom1.m(t));
lam_dom2.l(t) = abs(dom2.m(t));

* Transfer variable marginals to bound multipliers
piL_c.l(t)$(abs(c.l(t) - c.lo(t)) < 1e-6) = abs(c.m(t));
piL_l.l(t)$(abs(l.l(t) - l.lo(t)) < 1e-6) = abs(l.m(t));
piL_u.l(t)$(abs(u.l(t) - u.lo(t)) < 1e-6) = abs(u.m(t));
piU_l.l(t)$(abs(l.l(t) - l.up(t)) < 1e-6) = abs(l.m(t));

* Transfer fixed-variable marginals to _fx_ multipliers (#1462)
nu_a_fx_0.l = a.m('0');
nu_m_fx_0.l = m.m('0');

* Reset any NA/UNDF warm-start multiplier levels to 0 (#1322)
nu_utility.l(t)$(NOT (nu_utility.l(t) > -inf and nu_utility.l(t) < inf)) = 0;
nu_income.l(t)$(NOT (nu_income.l(t) > -inf and nu_income.l(t) < inf)) = 0;
nu_taxes.l(t)$(NOT (nu_taxes.l(t) > -inf and nu_taxes.l(t) < inf)) = 0;
nu_savings.l(t)$(NOT (nu_savings.l(t) > -inf and nu_savings.l(t) < inf)) = 0;
nu_budget.l(tl)$(NOT (nu_budget.l(tl) > -inf and nu_budget.l(tl) < inf)) = 0;
nu_timemoney.l(t)$(NOT (nu_timemoney.l(t) > -inf and nu_timemoney.l(t) < inf)) = 0;
nu_terminal.l(tt)$(NOT (nu_terminal.l(tt) > -inf and nu_terminal.l(tt) < inf)) = 0;
lam_dom1.l(t)$(NOT (lam_dom1.l(t) > -inf and lam_dom1.l(t) < inf)) = 0;
lam_dom2.l(t)$(NOT (lam_dom2.l(t) > -inf and lam_dom2.l(t) < inf)) = 0;
piL_c.l(t)$(NOT (piL_c.l(t) > -inf and piL_c.l(t) < inf)) = 0;
piL_l.l(t)$(NOT (piL_l.l(t) > -inf and piL_l.l(t) < inf)) = 0;
piL_u.l(t)$(NOT (piL_u.l(t) > -inf and piL_u.l(t) < inf)) = 0;
piU_l.l(t)$(NOT (piU_l.l(t) > -inf and piU_l.l(t) < inf)) = 0;
nu_a_fx_0.l$(NOT (nu_a_fx_0.l > -inf and nu_a_fx_0.l < inf)) = 0;
nu_m_fx_0.l$(NOT (nu_m_fx_0.l > -inf and nu_m_fx_0.l < inf)) = 0;

* ============================================
* #1449 (Layer 4): unfix elements fixed by the source $include but
* enforced in the MCP via an active _fx_ complementarity equation
* (else PATH drops the fixed column, leaving the _fx_ row unmatched).
* ============================================
a.lo('0') = -inf;
a.up('0') = +inf;
m.lo('0') = -inf;
m.up('0') = +inf;

* ============================================
* #1449: presolve widened-variable companions
* (avoid $184: the source $include declares these at their
*  subset domain; the MCP stationarity uses the <v>__pw
*  companion, bound to the source var on the subset by couple_<v>)
* ============================================
Free Variable n__pw(tl);
Equation couple_n(t);
couple_n(t).. n__pw(t) =e= n(t);
n__pw.fx(tl)$(not ((t(tl)))) = 0;

* ============================================
* Equations
* ============================================

* Stationarity: One equation per primal variable (except objvar)
* Complementarity: Equations for inequalities and bounds
* Equality constraints: Original equality constraints

Equations
    stat_a(tl)
    stat_c(t)
    stat_l(t)
    stat_m(tl)
    stat_n(t)
    stat_s(tl)
    stat_tax(t)
    stat_u(t)
    stat_y(t)
    comp_dom1(t)
    comp_dom2(t)
    comp_lo_c(t)
    comp_lo_l(t)
    comp_lo_u(t)
    comp_up_l(t)
    a_fx_0
    budget(tl)
    income(t)
    m_fx_0
    objective
    savings
    taxes(t)
    terminal(t)
    timemoney(t)
    utility(t)
;

* ============================================
* Equation Definitions
* ============================================

* Index aliases to avoid 'Set is under control already' error
* (GAMS Error 125 when equation domain index is reused in sum)
Alias(t, t__);

$onMultiR
* Stationarity equations
stat_a(tl).. ((-1) * nu_budget(tl)) + nu_budget(tl+1)$(ord(tl) <= card(tl) - 1) + nu_a_fx_0$(sameas(tl, '0')) + (((-1) * r) * nu_income(tl))$(t(tl)) + sum(tt, nu_terminal(tt)$(tt(tt))) =E= 0;
stat_c(t).. ((-1) * (100 * (a1 * c(t) ** ((-1) * a2) + (1 - a1) * (th - l(t) - n(t)) ** ((-1) * a2)) ** ((-1) / a2) * (-1) / a2 / (a1 * c(t) ** ((-1) * a2) + (1 - a1) * (th - l(t) - n(t)) ** ((-1) * a2)) * a1 * c(t) ** ((-1) * a2) * ((-1) * a2) / c(t) / 10000)) * nu_utility(t) + (p * nu_savings(t))$(t(t)) + (n(t) * ((-1) * (gamma1 * p)) * nu_timemoney(t))$(t(t)) + (1.01 * gamma1 * p * lam_dom1(t))$(t(t)) - piL_c(t) =E= 0;
stat_l(t).. ((-1) * (100 * (a1 * c(t) ** ((-1) * a2) + (1 - a1) * (th - l(t) - n(t)) ** ((-1) * a2)) ** ((-1) / a2) * (-1) / a2 / (a1 * c(t) ** ((-1) * a2) + (1 - a1) * (th - l(t) - n(t)) ** ((-1) * a2)) * (1 - a1) * (th - l(t) - n(t)) ** ((-1) * a2) * ((-1) * a2) / (th - l(t) - n(t)) * (-1) / 10000)) * nu_utility(t) + (((-1) * w) * nu_income(t))$(t(t)) + lam_dom2(t) - piL_l(t) + piU_l(t) =E= 0;
stat_m(tl).. ((-1) * nu_budget(tl)) + nu_budget(tl+1)$(ord(tl) <= card(tl) - 1) + nu_m_fx_0$(sameas(tl, '0')) + (n__pw(tl) * nu_timemoney(tl))$(t(tl)) + sum(tt, nu_terminal(tt)$(tt(tt))) - lam_dom1(tl)$(t(tl)) =E= 0;
stat_n(t).. ((-1) * (100 * (a1 * c(t) ** ((-1) * a2) + (1 - a1) * (th - l(t) - n(t)) ** ((-1) * a2)) ** ((-1) / a2) * (-1) / a2 / (a1 * c(t) ** ((-1) * a2) + (1 - a1) * (th - l(t) - n(t)) ** ((-1) * a2)) * (1 - a1) * (th - l(t) - n(t)) ** ((-1) * a2) * ((-1) * a2) / (th - l(t) - n(t)) * (-1) / 10000)) * nu_utility(t) + ((m(t) - gamma1 * p * c(t)) * nu_timemoney(t))$(t(t)) + lam_dom2(t) =E= 0;
stat_s(tl).. (nu_budget(tl) + nu_savings(tl)$(t(tl)))$(t(tl)) =E= 0;
stat_tax(t).. nu_taxes(t) + nu_savings(t)$(t(t)) =E= 0;
stat_u(t).. ((-1) * (prod(t__, u(t__) ** ufact(t__)) * u(t) ** ufact(t) * ufact(t) / u(t) / u(t) ** ufact(t))) + nu_utility(t) - piL_u(t) =E= 0;
stat_y(t).. nu_income(t)$(t(t)) + ((-1) * d) * nu_taxes(t) - nu_savings(t)$(t(t)) =E= 0;

* Inequality complementarity equations
comp_dom1(t).. m(t) - 1.01 * gamma1 * p * c(t) =G= 0;
comp_dom2(t).. ((-1) * (l(t) + n(t) - 0.9 * th)) =G= 0;

* Lower bound complementarity equations
comp_lo_c(t).. c(t) - 100 =G= 0;
comp_lo_l(t).. l(t) - 100 =G= 0;
comp_lo_u(t).. u(t) - 0.01 =G= 0;

* Upper bound complementarity equations
comp_up_l(t).. 400 - l(t) =G= 0;

* Original equality equations
objective.. obj =E= prod(t, u(t) ** ufact(t));
utility(t).. u(t) =E= (a1 * c(t) ** ((-1) * a2) + (1 - a1) * (th - l(t) - n(t)) ** ((-1) * a2)) ** ((-1) / a2) / 100;
income(t).. y(t) =E= w * l(t) + r * a(t);
taxes(t).. tax(t) =E= d * y(t) - tr;
savings(t).. s(t) =E= y(t) - tax(t) - p * c(t);
budget(tl)$(ord(tl) > 1).. s(tl) =E= a(tl) - a(tl-1) + m(tl) - m(tl-1);
timemoney(t).. n(t) * (m(t) - gamma1 * p * c(t)) =E= gamma2;
terminal(tt).. a(tt) + m(tt) =E= am;
a_fx_0.. a("0") - 1000 =E= 0;
m_fx_0.. m("0") - 100 =E= 0;

$offMulti

* ============================================
* Fix inactive variable instances
* ============================================

* Variables whose paired MCP equation is conditioned must be
* fixed for excluded instances to satisfy MCP matching.

s.fx(tl)$(not (t(tl))) = 0;
s.fx(tl)$(not (ord(tl) > 1)) = 0;
nu_budget.fx(tl)$(not (ord(tl) > 1)) = 0;
lam_dom1.fx(tl)$(not (t(tl))) = 0;
nu_income.fx(tl)$(not (t(tl))) = 0;
nu_savings.fx(tl)$(not (t(tl))) = 0;
nu_terminal.fx(t)$(not (tt(t))) = 0;
nu_timemoney.fx(tl)$(not (t(tl))) = 0;

* ============================================
* Model MCP Declaration
* ============================================

* Each line pairs an equation with a variable:
*   equation.variable
*
* This defines the complementarity problem:
*   equation ⊥ variable
*
* Meaning: equation = 0 if variable > 0
*          equation ≥ 0 if variable = 0

Model mcp_model /
    stat_a.a,
    stat_c.c,
    stat_l.l,
    stat_m.m,
    stat_n.n,
    stat_s.s,
    stat_tax.tax,
    stat_u.u,
    stat_y.y,
    comp_dom1.lam_dom1,
    comp_dom2.lam_dom2,
    a_fx_0.nu_a_fx_0,
    budget.nu_budget,
    income.nu_income,
    m_fx_0.nu_m_fx_0,
    objective.obj,
    savings.nu_savings,
    taxes.nu_taxes,
    terminal.nu_terminal,
    timemoney.nu_timemoney,
    utility.nu_utility,
    comp_lo_c.piL_c,
    comp_lo_l.piL_l,
    comp_lo_u.piL_u,
    comp_up_l.piU_l,
    couple_n.n__pw
/;

* ============================================
* Solve Statement
* ============================================

Solve mcp_model using MCP;

Scalar nlp2mcp_obj_val;
nlp2mcp_obj_val = obj.l;
Display nlp2mcp_obj_val;