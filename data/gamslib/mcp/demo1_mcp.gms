$onText
Generated by nlp2mcp

This file contains the KKT (Karush-Kuhn-Tucker) conditions
for the original NLP model, transformed into MCP format.

KKT System Components:
  - Stationarity: ∇f + J^T λ + J^T ν - π^L + π^U = 0
  - Complementarity: g(x) ⊥ λ, h(x) = 0, bounds ⊥ π
  - Dual feasibility: λ, π^L, π^U ≥ 0
  - Primal feasibility: g(x) ≤ 0, h(x) = 0, lo ≤ x ≤ up
$offText

* ============================================
* Original Model Declarations
* ============================================

Sets
    c /wheat, clover, beans, onions, cotton, maize, tomato/
    t /jan, feb, mar, apr, may, jun, jul, aug, sep, oct, nov, dec/
    crep /landuse, output, revenue/
    lrep /demand, family, temporary, unused, 'hire-out'/
;

Parameters
    landreq(t,c) /jan.wheat 1, jan.clover 1, jan.beans 1, jan.onions 1, feb.wheat 1, feb.clover 1, feb.beans 1, feb.onions 1, mar.wheat 1, mar.clover 0.5, mar.beans 1, mar.onions 1, mar.cotton 0.5, apr.wheat 1, apr.beans 1, apr.onions 1, apr.cotton 1, may.wheat 1, may.onions 0.25, may.cotton 1, may.maize 0.25, jun.cotton 1, jun.maize 1, jul.cotton 1, jul.maize 1, jul.tomato 0.75, aug.cotton 1, aug.maize 1, aug.tomato 1, sep.cotton 1, sep.maize 1, sep.tomato 1, oct.cotton 1, oct.maize 0.5, oct.tomato 1, nov.wheat 0.5, nov.clover 0.25, nov.beans 0.25, nov.onions 0.5, nov.cotton 0.75, nov.tomato 0.75, dec.wheat 1, dec.clover 1, dec.beans 1, dec.onions 1/
    laborreq(t,c) /jan.wheat 1.72, jan.clover 4.5, jan.beans 0.75, jan.onions 5.16, feb.wheat 0.5, feb.clover 1, feb.beans 0.75, feb.onions 5, mar.wheat 1, mar.clover 8, mar.beans 0.75, mar.onions 5, mar.cotton 5, apr.wheat 1, apr.beans 16, apr.onions 19.58, apr.cotton 5, may.wheat 17.16, may.onions 2.42, may.cotton 9, may.maize 4.3, jun.wheat 2.34, jun.cotton 2, jun.maize 5.04, jul.cotton 1.5, jul.maize 7.16, jul.tomato 17, aug.cotton 2, aug.maize 7.97, aug.tomato 15, sep.cotton 1, sep.maize 4.41, sep.tomato 12, oct.cotton 26, oct.maize 1.12, oct.tomato 7, nov.wheat 2.43, nov.clover 2.5, nov.beans 7.5, nov.onions 11.16, nov.cotton 12, nov.tomato 6, dec.wheat 1.35, dec.clover 7.5, dec.beans 0.75, dec.onions 4.68/
    yield(c) /wheat 1.5, clover 6.5, beans 1, onions 3, cotton 1.5, maize 2, tomato 3/
    price(c) /wheat 100, beans 200, onions 125, cotton 350, maize 70, tomato 120/
    miscost(c) /wheat 10, beans 5, onions 50, cotton 80, maize 5, tomato 50/
;

Scalars
    land /4/
    famlab /25/
    owage /3/
    twage /4/
    dpm /25/
;

* ============================================
* Variables (Primal + Multipliers)
* ============================================

* Primal variables: Original decision variables from the NLP
* Multipliers:
*   ν (nu_*): Free multipliers for equality constraints
*   λ (lam_*): Positive multipliers for inequality constraints
*   π^L (piL_*): Positive multipliers for lower bounds
*   π^U (piU_*): Positive multipliers for upper bounds

Variables
    yfarm
    revenue
    mcost
    labcost
    labearn
    nu_flabor(t)
    nu_arev
    nu_acost
    nu_alab
    nu_aout
;

Positive Variables
    xcrop(c)
    flab(t)
    fout(t)
    tlab(t)
    lam_landbal(t)
    lam_laborbal(t)
    piL_xcrop(c)
    piL_flab(t)
    piL_fout(t)
    piL_tlab(t)
;

* ============================================
* Variable Initialization
* ============================================

* Initialize variables to avoid division by zero during model generation.
* Variables appearing in denominators (from log, 1/x derivatives) need
* non-zero initial values.
* POSITIVE variables are set to 1.

xcrop.l(c) = 1;
flab.l(t) = 1;
fout.l(t) = 1;
tlab.l(t) = 1;

* ============================================
* Equations
* ============================================

* Stationarity: One equation per primal variable (except objvar)
* Complementarity: Equations for inequalities and bounds
* Equality constraints: Original equality constraints

Equations
    stat_flab(t)
    stat_fout(t)
    stat_labcost
    stat_labearn
    stat_mcost
    stat_revenue
    stat_tlab(t)
    stat_xcrop(c)
    comp_laborbal(t)
    comp_landbal(t)
    comp_lo_flab(t)
    comp_lo_fout(t)
    comp_lo_tlab(t)
    comp_lo_xcrop(c)
    acost
    alab
    aout
    arev
    flabor(t)
    income
;

* ============================================
* Equation Definitions
* ============================================

* Stationarity equations
stat_flab(t).. ((-1) * nu_flabor(t)) - lam_laborbal(t) - piL_flab(t) =E= 0;
stat_fout(t).. ((-1) * nu_flabor(t)) + ((-1) * owage) * nu_aout - piL_fout(t) =E= 0;
stat_labcost.. 1 + nu_alab =E= 0;
stat_labearn.. -1 + nu_aout =E= 0;
stat_mcost.. 1 + nu_acost =E= 0;
stat_revenue.. -1 + nu_arev =E= 0;
stat_tlab(t).. ((-1) * twage) * nu_alab - lam_laborbal(t) - piL_tlab(t) =E= 0;
stat_xcrop(c).. ((-1) * (price(c) * yield(c))) * nu_arev + ((-1) * miscost(c)) * nu_acost + sum(t, landreq(t,c) * lam_landbal(t)) + sum(t, laborreq(t,c) * lam_laborbal(t)) - piL_xcrop(c) =E= 0;

* Inequality complementarity equations
comp_laborbal(t).. ((-1) * (sum(c, xcrop(c) * laborreq(t,c)) - (flab(t) + tlab(t)))) =G= 0;
comp_landbal(t).. ((-1) * (sum(c, xcrop(c) * landreq(t,c)) - land)) =G= 0;

* Lower bound complementarity equations
comp_lo_flab(t).. flab(t) - 0 =G= 0;
comp_lo_fout(t).. fout(t) - 0 =G= 0;
comp_lo_tlab(t).. tlab(t) - 0 =G= 0;
comp_lo_xcrop(c).. xcrop(c) - 0 =G= 0;

* Original equality equations
flabor(t).. famlab =E= flab(t) + fout(t);
arev.. revenue =E= sum(c, xcrop(c) * yield(c) * price(c));
acost.. mcost =E= sum(c, xcrop(c) * miscost(c));
alab.. labcost =E= sum(t, tlab(t) * twage);
aout.. labearn =E= sum(t, fout(t) * owage);
income.. yfarm =E= revenue + labearn - labcost - mcost;


* ============================================
* Model MCP Declaration
* ============================================

* Each line pairs an equation with a variable:
*   equation.variable
*
* This defines the complementarity problem:
*   equation ⊥ variable
*
* Meaning: equation = 0 if variable > 0
*          equation ≥ 0 if variable = 0

Model mcp_model /
    stat_flab.flab,
    stat_fout.fout,
    stat_labcost.labcost,
    stat_labearn.labearn,
    stat_mcost.mcost,
    stat_revenue.revenue,
    stat_tlab.tlab,
    stat_xcrop.xcrop,
    comp_laborbal.lam_laborbal,
    comp_landbal.lam_landbal,
    acost.nu_acost,
    alab.nu_alab,
    aout.nu_aout,
    arev.nu_arev,
    flabor.nu_flabor,
    income.yfarm,
    comp_lo_flab.piL_flab,
    comp_lo_fout.piL_fout,
    comp_lo_tlab.piL_tlab,
    comp_lo_xcrop.piL_xcrop
/;

* ============================================
* Solve Statement
* ============================================

Solve mcp_model using MCP;

Scalar nlp2mcp_obj_val;
nlp2mcp_obj_val = yfarm.l;
Display nlp2mcp_obj_val;