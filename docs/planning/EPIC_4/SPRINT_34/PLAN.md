# Sprint 34 Detailed Schedule (Day 0 + Days 1–13)

**Created:** 2026-07-20
**Prep Task:** 11 (the final prep task — integrates Tasks 1–10)
**Budget:** 88–130 h work-items over 14 days (Day 0 + Days 1–13) at ≤ 12 h/day (168 h cap); Risk **HIGH**.
**Day-0 code anchor:** `750803b2` (Sprint 33 close, PR #1581). **DB:** the S33-close DB (`data/gamslib/gamslib_status.json`, md5 `6166acab…`); the old byte-anchor `4cbf8bff` (S31 close) is **historical** — the S33 Day-11 P6 sample fix moved the DB off it (last DB-modifying commit `1568a531`).

---

## 1. Sprint 34 Goal

Land the five Sprint-33 REPLAN'd/deferred carryforwards, each carrying a **Sprint-33 control-confirmed characterization** (`SPRINT_33/SPRINT_RETROSPECTIVE.md` + `SPRINT_33/SPRINT_34_CARRYFORWARDS.md`) but an **un-built** fix — and, for P1/P3, a *harder-than-a-keying-tweak* one: **P1 mine head-offset dual subsystem** (#1443), **P2 sarf three-site symbolic `stat_task` emit mode** (#1385), **P3 fawley second-index `sameas` correction + forcing** (#1111/#1112), the **NEW P4 max-convention bound-transfer-sign track**, and **P5 camcge dual-consistent Walras** (#1330 → Epic 5) + **rocket PATH** (#1462 → Sprint 35). The front-loaded deep + fresh tracks — **P1 mine + P2 sarf + P4 bound-transfer** — lead so a REPLAN surfaces by the **Day-5 checkpoint** (the S33 lesson: front-loading mine Day 2 + fawley Day 4/5 + the sarf Day-0 probe surfaced every deep-track REPLAN by the checkpoint, freeing the back half for the P6 sample win). **The two escalations Sprint 34 carries in (Task 9):** (1) **P1's banked premise was twice-refuted in prep** — S32 refuted the `N`-derivation, and S33 proved both the `stat_x` cross-term *algebraically correct* and H1 head-label re-keying **value-invariant** — so mine enters on an **unproven third hypothesis (H_dual — a structural complementarity-pairing change)** against a gate **reframed to the cold MCP reaching MS-1** (not the warm residual `N→0`, which no keying/pairing change can move), at a **High** REPLAN prior; (2) **P3's +Solve is confirmed NOT in-sprint** (H-b: sameas + all bound-transfer signs → warm `max|stat_bq| ~0` but MCP still MS-5 @ 4399.557) — its correctness fix ships, but the +Solve hands to the P5 forcing survey and its genuine-floor +1 is contingent on that forcing.

## 2. Acceptance Criteria (from `PROJECT_PLAN.md` §"Sprint 34" + `BASELINE_METRICS.md`)

- **Solve 108 → +1 (109)** conditional on **P1 mine [cold-MS-1 @ 17500] OR P4 bound-transfer [agreste, contingent] OR P6 [ganges/gangesx]** landing a bucket move; **NOT** P3 (its +Solve is a P5 forcing hand-off, a priori unpromising), **NOT** camcge (Epic-5), **NOT** sarf (Translate). **Solve ≥ 110 is a further stretch** needing multiple movers — a priori unlikely. **model_infeasible ≤ 7 / path_syntax_error ≤ 7** maintain (−1 per landed mover).
- **Match maintain ≥ 93** as-measured (all-219 Match 96); **genuine floor 75 → +1 (76)** conditional on **P1 OR P3 [contingent on forcing] OR P6 COLD-matching** an emit change — **NOT** presolve-methodology reclassification (**P5 delivers 0 floor**; **P4 delivers 0 floor directly** — any floor gain routes through a +Solve).
- **Translate 135 → +1 (136)** via #1385 sarf [P2]; **Parse 142** maintain. The lowest-leverage KPI (moves neither Solve nor Match).
- **Tests** up from ~5,034; **Determinism** byte-identical under ≥ 3 `PYTHONHASHSEED` (PR12).
- **The modal outcome is flat-KPI (Task 9)** — every deep-track mover is REPLAN-prone; the sprint's firm product is the de-risking (five control-confirmed dispositions, the Epic-5 camcge scope, the Sprint-35 rocket hand-off), **beaten (if at all) by the P6 failure-cohort** (ganges/gangesx `$141/$145/$149` — the designated best-remaining-shot, exactly the S33 sample shape).

## 3. Sequencing Constraints (from the prep-task outputs)

- **Front-load the deep + fresh tracks (P1 mine Days 1–3 + P4 bound-transfer Day 4 + P3 fawley Day 5)** so their PROCEED/REPLAN gates all fire by the **Day-5 checkpoint** (Task 9 §Front-load). **P2 sarf's REPLAN risk is retired earliest of all** — its **O(active=398) timing probe is Day 0** (the cheapest verdict in the sprint); the atomic three-site build follows Days 6–9 (its dominant risk is already resolved). P5-camcge's Epic-5 `/tmp` gate + P5-rocket's Sprint-35 submission are low-cost, placed at Day 10 (the Day-0 clean-at-NLP re-confirm precedes them).
- **P1 mine is High-prior (Task 9):** the banked premise is **twice-refuted** (S32 `N`-derivation + S33 H1 value-invariance). The fix is **H_dual** — anchor the head-placed precedence dual's *complementarity* to the head-side variable (a **structural pairing** change via `head_domain_offsets`), **NOT** a term/sign re-derivation (refuted) and **NOT** a keying tweak (value-invariant). Run the H_dual `/tmp` structural prototype → the **cold-MS-1** gate **first** (Day 0/1). **`x.up=inf` is BANNED** (the S31 measurement error).
- **P3 fawley is H-b confirmed (Task 5/9):** the **genuine constraint-index-diagonal `sameas` correction** ships for **correctness** (473 → 18.468 control-proven; the fix surface is the general `sameas`-guard path in `_add_indexed_jacobian_terms`, `src/kkt/stationarity.py:5861`, **NOT** the 1-D polygon core `_var_at_two_indices_complement:7291`). But fawley's **+Solve is H-b** (emit closes yet MS-5 @ 4399.557; the residual-18.468 cc-dist cell is **P4**'s) → hands to the **P5 `--force` survey**; the +1 genuine floor is **contingent on forcing** (fawley does not cold-match under H-b). **REPLAN only** if the generalization leaks onto the mbal / variable-index-diagonal shape or regresses the 1-D core.
- **P4 bound-transfer is the fresh lever (Task 6/9):** the sign-robust `= abs(var.m)` at the **active bound** (Option B sense-aware — `ObjSense.MAX`-conditioned, so MINIMIZE stays byte-identical; the min-convention gates at `src/emit/emit_gams.py:1590`/`:1603`). **Honest finding:** the MAXIMIZE `model_infeasible` cohort is otherwise-attributed (fawley H-b, mine `x.m=0`, camcge Epic-5, rocket Case-c), so the realistic **+Solve target reduces to agreste** (P6-entangled). P4's firm value is the **general warm-start-correctness fix**; the +Solve is **contingent** (documented-finding exit if no candidate is warm-residual-driven — not a correctness REPLAN).
- **The genuine-floor ramp is conditional** (Task 9 / S30 §3 / S32 §3): genuine floor +1 advances only via an **emit-changing / recovered** cold-match (P1 H_dual, P3's correction *if forcing lands the solve*, or P6's ganges/gangesx recovery), **NOT** via presolve-methodology reclassification — **P5 = 0 floor, P4 = 0 floor directly**. Anchor **75**.
- **P5 camcge is Epic-5-deferred (expected); rocket is Sprint-35:** the in-sprint camcge work is the `/tmp` full-redefinition gate that **confirms** the MS-4 deferral (banked: price-pin MS-4, 3+ sprints of MS-4 variants; step 1 landed S32 PR #1553); rocket's FINALIZED PATH-consultation input is **submitted to the Sprint-35 consultation**. **0 genuine floor; the Case-c sign flip is BANNED** (refuted 4× S30–S31).
- **Reallocation order on any REPLAN (Task 9):** **P6** (the ganges/gangesx `$141/$145/$149` cohort + agreste — the designated best-remaining-shot) → **P7** (property fixtures + genuine-floor tracking + the Epic-4 `SUMMARY` continuation) → the rocket [P5] Sprint-35 tail. The offset-alias generalization is **exhausted** (Task 10 — cpack et al. already solve; fawley promoted to P3).

## 4. Day 0 — Kickoff + Day-0 Traces + Control Probes (≤ 6 h)

- Confirm Day-0 = Sprint 33 close (`BASELINE_METRICS.md`: **Solve 108 / Match 93 / genuine floor 75 / model_infeasible 7 / path_syntax_error 7 / Translate 135 / all-219 Match 96 / Tests ~5,034**). **Verify** `git diff 750803b2..HEAD -- src/ scripts/` is empty before skipping the retest; if non-empty, run a fresh retest.
- **Day-0 traces (PR24)** — re-confirm each Phase-0 gate's Day-0 fingerprint (`PHASE_0_ACCEPTANCE_GATES.md` §1): mine (`kkt_residual.py` CASE_B `stat_x(3,1,1)` rel 2.37, dual CONSISTENT — **and** the Task-3 findings: the cross-term is *algebraically correct* and H1 re-keying **value-invariant**; the residual is the **22-row** `c`-boundary + `d\c`-ring), sarf (the three enumeration sites + the 369K-vs-398-active count + the live **> 116s** blow-up), fawley (CASE_B `stat_bq(res-arab-l,fuel-oil)` 0.973 raw 473; the constraint-index-diagonal control 473 → 18.468 [the 18.468 is the P4 cc-dist cell]), bound-transfer (the min-convention gates at `src/emit/emit_gams.py:1590`/`:1603`; `bq.m=-18.468` at a lower bound + the mine 3 upper-bound `x.m>0` rows), camcge (CASE_B `stat_mps` cleared by S32 step 1; the residual MS-4 Walras), rocket (Case-c clean at the NLP point — boundary `stat_ht(h0)`/`stat_ht(h50)`/`stat_step`, dual CONSISTENT).
- **The control probes (Task 9 single-model validations / Task 8 gates), run BEFORE any `src/`:**
  - **P2 sarf O(active) timing probe (run FIRST — the cheapest, earliest verdict):** confirm the sparsified `stat_task(g,t,m,n)$taskposs(g,t)` enumerates **398 active**, not 369,024 Cartesian — a lightweight count/scoping probe that retires the sprint's dominant translate-timeout risk Day 0 (the full timing gate is Day 7).
  - **P1 H_dual `/tmp` structural prototype (highest prior):** anchor `comp_pr`/`lam_pr`'s complementarity to the head-side variable at the shifted head label `(k,l+1,i,j)` via the unused `head_domain_offsets` IR, on a **scratch** `/tmp/mine_mcp_prototype.gms` + `/tmp/mine_mcp_presolve_prototype.gms` (**not** the committed `data/gamslib/mcp/mine_mcp.gms`), run from the **repo root** (the emit `$include "data/gamslib/raw/mine.gms"` is repo-relative). **Gate (reframed): the cold MCP reaches MS-1 @ 17500** (`modelstat=1` asserted; **`x.up=inf` BANNED**), the 22 boundary rows closing *in the cold solution* + interior rows unperturbed — **NOT** `N → 0` at the warm point (keying-invariant).
  - **P3 fawley localize-by-column `/tmp`:** re-confirm the constraint-index-diagonal control (`max|stat_bq|` 473 → 18.468) and confirm the residual-18.468 localizes to the **P4** cc-dist bound-transfer cell (so `max|stat_bq| → 0` needs **P3 + P4 together**).
  - **P4 bound-transfer `/tmp` cell:** confirm the sign-robust `= abs(var.m)` closes the fawley cc-dist warm cell (`bq.m=-18.468`, Day-4 proven) + the mine 3 upper-bound rows; scope the Option-B sense-aware guard.
  - **P5 camcge/rocket clean-at-NLP re-confirm:** camcge `stat_mps` Case-a (step 1 landed) + the residual MS-4 Walras; rocket residual clean at the NLP point (a *forcing* problem, not an emit bug).
- **PR25 Day-0 tally:** restate genuine 75 / methodology (anchor 75); the genuine-floor → +1 conversion map (P1 H_dual cold-match; P3's genuine correction *if forcing lands*; P6 ganges/gangesx cold-match — the firmest, the S33 precedent). **Docs/trace-only (no `src/`).**

## 5. Day 1 — Priority 1: mine H_dual head-offset dual reconciliation (start) (~7 h)

- **The H_dual structural complementarity-pairing change (Task 3 — the cross-term is already correct, H1 keying value-invariant).** Anchor the head-placed precedence dual `pr.m(k,l+1,i,j)`'s *complementarity* to the head-side variable via the `head_domain_offsets` IR carrier (`src/ir/symbols.py` field + `src/ir/parser.py` populate, both on `main` from S31) wired into `_try_build_param_offset_crossterm` (`src/kkt/stationarity.py:5712`) + the `_emit_nlp_presolve` transfer (`src/emit/emit_gams.py`). **Do NOT change the cross-term terms/signs** (refuted twice: S32 `N`-derivation + S33 Task-3) and **do NOT merely re-key the multiplier** (value-invariant, S33 Day-2). Gate to the head-offset-coupled case so the non-mine param-offset cohort (srpchase) stays byte-stable.
- **Phase-0 gate:** `PHASE_0_ACCEPTANCE_GATES.md` §1 P1 (the Day-0 H_dual `/tmp` cold-MS-1 prototype must have passed before `src/`). **Emit-touching PR (WIP if incomplete). Est ~7 h.**

## 6. Day 2 — Priority 1: mine cold-MS-1 verification (~7 h)

- **The cold-MCP gate (Unknown 1.2 — the reframed key correction).** Re-run `kkt_residual.py data/gamslib/raw/mine.gms` after the H_dual reconciliation, then solve the **cold** MCP → **MS-1 @ 17500** (`modelstat=1` asserted; **`x.up=inf` BANNED**), the 22 boundary rows closing *in the cold solution* + interior rows unperturbed (+1 Solve; +1 genuine floor if it cold-matches). **NB — the gate is the cold solve, NOT warm `N→0`** (keying/pairing-invariant). Confirm the S31 head-offset foundation guard tests stay green + the non-mine presolve goldens byte-stable (`--resolve-changed --since-commit 750803b2`).
- **Verifies (in-sprint):** the P1 gate. **Emit-touching PR. Est ~7 h.**

## 7. Day 3 — Priority 1: mine close-or-REPLAN (~6 h)

- **The deeper-dual-degeneracy gate (Unknown 1.2, H3′).** **PROCEED** (mine `model_infeasible → model_optimal`, +1 Solve; +1 genuine floor if cold-match) if the **cold** MCP reaches MS-1 @ 17500 with no interior perturbation and no srpchase regression. **REPLAN (prior High)** if H_dual cannot drive the cold MCP to MS-1 @ 17500 without perturbing an interior row or regressing srpchase → the boundary is a genuine dual-degeneracy the emit cannot deterministically reconcile → file a **deeper head-offset dual architecture** (or a PATH-consultation question) to a later sprint; mine stays `model_infeasible`. The `MINE_DUAL_SUBSYSTEM_DESIGN.md` + the S31 IR foundation (on `main`) hand off cleanly. Freed ~14–18 h → **P6 + P7** (Task 9 reallocation).
- **REPLAN exit explicit. PR. Est ~6 h.** *(P1 total ~20 h across Days 1–3.)*

## 8. Day 4 — Priority 4: max-convention bound-transfer-sign (sign-robust transfer + agreste survey) (~8 h)

- **The sign-robust `piL_*/piU_*` transfer (Task 6 — the fresh lever).** Implement the `= abs(var.m)` at the **active bound**, **Option B sense-aware** (`model_ir.objective.sense == ObjSense.MAX`; precedent `src/ad/gradient.py:300`), so MINIMIZE stays byte-identical and only MAXIMIZE goldens change — at the min-convention gates `src/emit/emit_gams.py:1590` (`piL`: `…and var.m > 0`) + `:1603` (`piU`: `…and var.m < 0`). The transfer fires **only** at active bounds (no over-transfer on interior/presolve-match models). Then the **+Solve survey (primarily agreste):** does the sign-robust transfer close the warm residual **AND** reach MS-1 (warm-residual-driven → +Solve) vs stay MS-5 (structural)?
- **REPLAN / documented-finding exit:** no candidate is warm-residual-driven (the a-priori-likely outcome) → the sign-robust transfer ships as a **general warm-start-correctness fix** with **no +Solve** (a documented finding, **not** a correctness REPLAN); OR the change over-transfers / regresses the ~20 MAXIMIZE presolve-match models (`--resolve-changed --since-commit 750803b2` NO-GO) → re-scope (Option B is the mitigation).
- **Phase-0 gate:** `PHASE_0_ACCEPTANCE_GATES.md` §1 P4. **Emit-touching PR. Est ~8 h.** *(P4 firm-correctness fix lands regardless; the +Solve disposition is known by Day 4.)*

## 9. Day 5 — Priority 3: fawley constraint-index-diagonal correction + Checkpoint 1 (~8 h)

- **The constraint-index-diagonal `sameas` correction (Task 5 — for correctness; NOT the 1-D polygon core).** Extend the general `sameas`-guard path (`src/kkt/stationarity.py` — `_build_sameas_guard`/`_get_or_create_fresh_alias` in `_add_indexed_jacobian_terms:5861`) so **every** qsb/pbal second-index `cfq` gets the `$(sameas(cfq__, cf))` restriction the mbal term already has, covering the 2-D cross-terms `bq(c,cf)` (473 → 18.468). **Do NOT touch the 1-D core** (`_var_at_two_indices_complement:7291` — polygon/ps2) and **no mbal-term change**. **The +Solve is a P5 forcing hand-off (H-b confirmed):** the correction ships for correctness (a cold-emit change), and the +1 genuine floor is **contingent on forcing** (fawley does not cold-match under H-b; `max|stat_bq| → 0` needs P3 + P4 together and *still* MS-5 @ 4399.557). **REPLAN only** if the generalization **leaks onto the mbal / variable-index-diagonal shape or regresses the 1-D polygon core** → re-scope; freed ~6–12 h → P6/P7.
- **Checkpoint 1 (Day 5):** `--resolve-changed --since-commit 750803b2` re-solve of the changed-golden set (bucket-diff vs the committed DB) + golden-staleness + the PR25 re-baseline. **NO-GO** if any changed-golden model moved backward (`match→mismatch`, `model_optimal→model_infeasible`, presolve-match→abort). **All front-loaded PROCEED/REPLAN gates have now fired: P1 cold-MS-1 (Day 3), P4 +Solve survey (Day 4), P3 correctness (Day 5); P2's timeout risk cleared at the Day-0 probe.**
- **Verifies (in-sprint):** the P3 gate + no-regression (Unknown 3.3). **REPLAN exit explicit. PR (emit-touching). Est ~8 h.** *(P3 total ~14 h across Day 0 control + Day 5; the design landed in prep.)*

## 10. Days 6–9 — Priority 2: sarf three-site symbolic `stat_task` emit (~24 h)

- **The atomic three-site O(active) symbolic re-emit (Task 4).** Eliminate the 369,024-column materialization at **all three** sites, landed **atomically** (a partial = an inconsistent MCP): S1 the `acost3` body-differentiation (`src/ad/constraint_jacobian.py`), S2 the variable-column enumeration (`src/ad/index_mapping.py`), S3 the variable stationarity (`src/kkt/stationarity.py`) — replaced by **one** symbolic guarded `stat_task(g,t,m,n)$taskposs(g,t)` (the banked 7-term derivation, Task-4-verified term-for-term) + `task.fx(g,t,m,n)$(not (taskposs(g,t) and tech(g,m,n))) = 0` over the **398 active** instances (a 927× reduction), **no set-name-literal multiplier indices** (the Sprint-26 `nu_slack("srn")` failure, commit `243fe578`; scan two greps — `grep -E 'nu_[[:alnum:]_]+\("' sarf_mcp.gms` and `grep -E 'lam_[[:alnum:]_]+\("' sarf_mcp.gms` — both empty).
- **The tractability gate (Unknown 2.2, Day 7).** The re-emit must be **O(active = 398), not O(369,024)** — time `sarf` translate (target **seconds**, cf. srpchase's 1-D analogue ~2.9s; the failure is > 116s). **PROCEED** (sarf `translate_failure → translate`, +1 Translate) if sub-budget; **REPLAN** (a documented parametric-emit re-scoping — a 4th enumeration site / builder-pipeline materialization; freed ~9–20 h → P6/P7) if it re-triggers the timeout. *(The Day-0 O(active) probe already retired the dominant risk; Day 7 is the full timing confirmation.)*
- **Day 9 close:** golden byte-stable (sarf's *new* `sarf_mcp.gms` golden — caught by the golden-staleness gate, since `--resolve-changed` diffs *existing* goldens) + deterministic ×3 `PYTHONHASHSEED`; `--resolve-changed --since-commit 750803b2` GO (sarf the only changed golden).
- **Verifies (in-sprint):** the P2 gate + the derivation completeness (Unknown 2.3). **REPLAN exit explicit. PR (emit-touching). Est ~24 h (~6/day).**

## 11. Day 10 — Priority 5 camcge Epic-5 gate + rocket Sprint-35 submission + Checkpoint 2 (~10 h)

- **P5 camcge Epic-5 `/tmp` gate (confirm the deferral — Task 7).** Run the `/tmp` prototype of the **full** dual-consistent redefinition (keep every market-clearing row + the consumption-weighted numéraire + **redefine the redundant market's dual via Walras' law**), checking the KKT **dual** side. **Expected: MS-4** (banked: price-pin MS-4, single-dual-pin MS-4, 3+ sprints of MS-4 variants) → **Epic-5-deferred** (camcge stays `model_infeasible`; the numéraire recipe + the S1∧S2∧S3 detector — which must flag **only** camcge, S3 = cold-MCP-singular-at-iter-0 the false-positive guard — are the de-risked Epic-5 hand-off). **Promote to +1 Solve only if** the prototype unexpectedly reaches **MS-1 at omega 191.7346**. Step 1 landed S32 (PR #1553).
- **P5 rocket Sprint-35 submission (Task 7 — no emit fix, sign flip BANNED).** Re-confirm rocket's residual is **clean at the NLP point** (the boundary signature, dual CONSISTENT) — a *forcing* problem, not an emit bug — then **submit** the FINALIZED PATH-consultation input to the **Sprint-35** consultation. The `--force` survey is exhausted (all MS-5). **0 genuine floor.**
- **Checkpoint 2 (Day 10):** `--resolve-changed --since-commit 750803b2` re-solve + golden-staleness + the PR25 tally.
- **Verifies (in-sprint):** the P5 gates (Unknowns 5.1–5.3). **PR (P5 docs unless the camcge `/tmp` unexpectedly crosses to emit). Est ~10 h.**

## 12. Day 11 — Priority 6: failure-cohort re-triage + REPLAN-slack (~11 h)

- **P6 ganges/gangesx `$141/$145/$149` cohort (Task 10 §3 — the designated best-remaining-shot).** The two share a single, live-confirmed root: the **NaN-sanitization emit pass** emits a self-referential guard `param(i)$(NOT (param(i) > -inf and param(i) < inf)) = 0` over parameters (adst/aex/aid/…) whose source value-assignment depends on a variable level `dst.l` (`$141` = "Symbol declared but no values assigned"; both models: `$141`×15 / `$145`×3 / `$149`×9). **A single fix may recover both**, distinct from sample's `$140` (pruned-var `.l`-init, the S33 P6 fix — a no-op here). Each `--resolve-changed --since-commit 750803b2`-gated + a golden-staleness check on the new goldens.
- **P6 agreste scope-verify (Task 10 §2).** Inspect the agreste source for the **double-`solve`** scope (two `solve … maximizing yfarm using lp`, `agreste.gms:294`/`:298`) BEFORE treating the CASE_B `stat_sales` as an emit bug (the multi-solve-gate lesson; agreste is P4-entangled). If the single-solve scope holds CASE_B → a +Solve candidate (`--resolve-changed`-gated); if a driver artifact → **bank**. The cohort is **multi-root** — verify per-model.
- **REPLAN-slack absorption:** whatever the P1/P2/P3/P4 REPLANs freed re-allocates here first (Task 9 order: P6 → P7 → the rocket tail).
- **Deliverable:** ≥ 1 model recovered (Solve/Match/genuine floor) OR the cohort re-triaged with banked diagnoses. **Verifies (in-sprint):** the P6 candidates (Unknowns 6.1/6.2/6.3). **PR (emit-touching if a candidate lands, else docs). Est ~11 h (heaviest day).**

## 13. Day 12 — Priority 7: infrastructure + REPLAN-slack (~8 h)

- **P7 property fixtures (Unknown 7.1) — each fail-before/pass-after, landing only once its track's fix landed:** **shape12** (head-offset dual — guards P1), **shape13** (sarf symbolic `stat_task` — guards P2), a **fawley 2-D second-index** fixture (guards P3, distinct from the 1-D shape10/11), plus an **optional P4 MAXIMIZE bound-transfer** fixture (added only if P4's correctness fix lands) → `tests/integration/emit/test_ad_crossterm_shapes.py` (+ the `test_sample_pruned_var_l_init.py` raw-emit + skip-if-absent pattern for any recovered P6 model).
- **Genuine-floor tracking + checkpoint refresh (Unknowns 7.1/7.3):** recompute the PR25 **genuine-floor tracking** (anchor **75**); refresh the `--resolve-changed` checkpoint targets for the newly-touched emit sites.
- **Epic-4 `SUMMARY.md` row-34 continuation (Unknown 7.3):** (1) **reconcile the theme cell** — the pre-insertion row currently holds the Quality/PATH-consultation theme (now **Sprint 35**); Sprint 34's is "Sprint 33 REPLAN'd/deferred carryforwards"; (2) **fill the cells** in the rows-28–33 format {Theme / Headline KPIs / Firm landing(s) / REPLAN'd → carryforward}; add row 35.
- **REPLAN-slack:** absorb residual freed budget per the Task-9 reallocation order.
- **Verifies (in-sprint):** the P7 infra (Unknowns 7.1/7.3). **PR (tests/ + docs). Est ~8 h.**

## 14. Day 13 — Final Retest + Closeout (~8 h)

- **Full pipeline retest** under ≥ 3 `PYTHONHASHSEED` values (PR12); recompute the DB (machine-portable paths) + the Sprint 33 → 34 metrics comparison; **PR25 genuine-vs-methodology re-baseline** recomputed (genuine floor anchor 75).
- **Closeout:** `SPRINT_LOG.md` final entry + top-table + per-priority summary; `SPRINT_RETROSPECTIVE.md` authored; the Sprint-35 carryforwards filed (mine if REPLAN'd → deeper head-offset dual architecture; sarf if REPLAN'd → re-scoping; fawley +Solve → the P5 forcing tail if not landed; the camcge numéraire → Epic 5; the rocket PATH-consultation input → the Sprint-35 consultation; cesam/lnts Case-c; any un-landed P6 candidate). **Fill the SUMMARY row-34 cells (Day-12 continuation). Est ~8 h.**

---

## 15. Budget Summary

| Day(s) | Track | Est (h) |
|---|---|---|
| 0 | Kickoff + Day-0 traces + control probes (P2 O(active) probe FIRST, P1 H_dual, P3 localize, P4 abs-cell, P5 clean-at-NLP) | ~6 |
| 1–3 | **P1 mine head-offset dual subsystem** (H_dual reconciliation; cold-MS-1 verify Day 2; close-or-REPLAN Day 3) | ~20 |
| 4 | **P4 max-convention bound-transfer** (sign-robust `abs(var.m)`, Option B sense-aware; agreste +Solve survey) | ~8 |
| 5 | **P3 fawley constraint-index-diagonal `sameas` correction** (H-b; +Solve → P5 forcing) + Checkpoint 1 | ~8 |
| 6–9 | P2 sarf three-site symbolic `stat_task` emit (tractability gate Day 7) | ~24 |
| 10 | P5 camcge Epic-5 `/tmp` gate + rocket Sprint-35 submission + Checkpoint 2 | ~10 |
| 11 | P6 failure-cohort (ganges/gangesx `$141/$145/$149` + agreste) + REPLAN-slack | ~11 |
| 12 | P7 infrastructure (shape12/13/fawley + optional P4 fixture + tracking + SUMMARY row-34) + REPLAN-slack | ~8 |
| 13 | Final retest (≥ 3 seeds) + closeout | ~8 |
| **Total** | | **~103 h** (mid; ~88 h if the deep tracks REPLAN early, ~130 h if all PROCEED) |

**Fits the 168 h cap** with ≥ 65 h slack at the mid-estimate; **no day > 12 h** (heaviest ~11 h on Day 11, the P6 failure-cohort + REPLAN-slack day). The per-priority **work-item** sizings (`PROJECT_PLAN.md` §"Sprint 34"; these span design [done in prep] + build + verify + REPLAN-contingency + the Day-0 probes and Day-11/12 fixtures, so they do not partition the per-day schedule one-to-one — the same dual-accounting view as `SPRINT_33/PLAN.md`): P1 [18–24h] + P2 [20–28h] + P3 [12–18h] + P4 [10–16h] + P5 [8–14h] + P6 [10–16h] + P7 [6–10h] + retest [4h] = **88–130h**. The lower bound assumes the deepest from-scratch tracks (P1 dual subsystem, P2 symbolic-emit mode) slip per Task 9; the **firm parts land regardless** — the mine H_dual design + characterization, the sarf three-site O(active) design (or its re-scoping finding), the fawley genuine correction (even under H-b), the P4 general warm-start-correctness fix, the P5 Epic-5 recipe + detector + the rocket consultation input, the P6 banked diagnoses (or a recovery), the P7 infra.

## 16. Phase 0 Coverage Audit (PR20 + PR24 + PR27)

The four emit-touching tracks (P1 mine, P2 sarf, P3 fawley, P4 bound-transfer) each have a PROCEED/REPLAN gate in `PHASE_0_ACCEPTANCE_GATES.md` §1. Each gate's Day-0 fingerprint is re-confirmed Day 0 before any `src/` change; each cites `kkt_residual.py` (PR27) + a control-before-`src/` rule (P1 the H_dual `/tmp` **cold-MS-1** prototype — **not** warm `N→0`, keying-invariant; P3 the constraint-index-diagonal 473→18.468 localize-by-column; P4 the sign-robust `abs(var.m)` cc-dist/mine-row cell; P2 the O(active=398) probe). P5 camcge is an Epic-5 `/tmp` gate (confirm the MS-4 deferral, no in-sprint emit); P5 rocket is a docs hand-off (no emit); P6 candidates each pass the `--resolve-changed --since-commit 750803b2` GO gate before landing. **`modelstat` is asserted before every objective read; `x.up=inf` is BANNED (mine); the objective-gradient sign flip is BANNED (Case-c, refuted 4×).**

## 17. Known Unknowns Status Snapshot

All 27 prep unknowns are resolved (Tasks 2–10), covering the mine head-offset dual (1.1–1.5), the sarf three-site O(active) emit (2.1–2.5), the fawley second-index correction (3.1–3.4), the max-convention bound-transfer (4.1–4.4), the camcge Epic-5 Walras + rocket (5.1–5.3), the P6 failure-cohort (6.1–6.3), and the P7 infra (7.1–7.3). **The sole `🔍 INCOMPLETE` is Unknown 1.2 — and it is intentionally DESIGN-SPECIFIED:** the mine H_dual reconciliation design + the cold-MS-1 `/tmp` control are fully specified, but the control is the Sprint-34 **Day-1 executed gate** (it cannot run in a docs-only prep) — so its resolution is an **in-sprint execution gate, NOT a Day-0 blocker**. **No Critical/High unknown is an unresolved prep blocker.** The in-sprint gates that remain are the **execution** of each track's PROCEED/REPLAN gate (Days 2/4/5/7), not open prep questions.

## 18. Risk Register + Mitigations

| Risk | Mitigation |
|---|---|
| Solve +1 misses (in-sprint movers = {mine [P1, cold-MS-1], bound-transfer [P4, agreste — contingent], ganges/gangesx [P6]}) | Honest projection (Task 9): P1/P4 are front-loaded Days 1–4 so a REPLAN surfaces at Checkpoint 1; freed budget → P6 (ganges/gangesx — the designated best-remaining-shot, the S33 sample precedent). P3's +Solve is a P5 forcing hand-off (not in-sprint); camcge Epic-5; sarf Translate. |
| P1 mine's H_dual surfaces a deeper dual-degeneracy (High prior — premise twice-refuted) | Explicit REPLAN mine → a deeper head-offset dual architecture (Day 3); the cross-term-correct + H1-value-invariant findings + the `MINE_DUAL_SUBSYSTEM_DESIGN.md` + the S31 IR foundation hand off cleanly. Run the H_dual `/tmp` cold-MS-1 control **first** (Day 0). **`x.up=inf` BANNED.** |
| P3 fawley closes the emit but the MCP stays MS-5 (H-b confirmed) | **Not a REPLAN** — the genuine constraint-index-diagonal correction ships (a floor lever, a cold-emit change); only the +Solve defers to the P5 forcing survey and the +1 floor is contingent on it. REPLAN only on an mbal / 1-D-core leak. |
| P4 bound-transfer ships but no MAXIMIZE candidate is warm-residual-driven | **The a-priori outcome — not a correctness REPLAN:** the sign-robust transfer ships as a general warm-start-correctness fix (a documented finding, no +Solve). Over-transfer / MAXIMIZE-presolve regression → re-scope (Option B `ObjSense.MAX` is the mitigation; `--resolve-changed` NO-GO catches it). |
| P2 sarf symbolic re-emit re-triggers the translate timeout (a 4th site) | The O(active=398) probe (Day 0) retires the dominant risk earliest of all; the tractability gate (Day 7) confirms; REPLAN → a documented re-scoping; +Translate deferred. |
| camcge Walras `/tmp` stays MS-4 (Epic-5 dual rank-deficiency) | **Expected** — camcge is Epic-5-deferred; the `/tmp` gate confirms the deferral; step 1 (on `main`) + the numéraire recipe + the S1∧S2∧S3 detector (flags camcge only) are the de-risked Epic-5 hand-off. No in-sprint `src/`. |
| genuine floor +1 counted from presolve-methodology | Task 9 / S32 §3: the floor advances only via an emit-changing / recovered cold-match (P1 H_dual, P3's correction *if forcing lands*, or P6); **P5 = 0 floor, P4 = 0 floor directly**. Anchor 75. |
| a P6 emit change regresses the 93-match / 108-solve guard | Each P6 candidate passes the `--resolve-changed --since-commit 750803b2` GO gate before landing (NO-GO → revert, no net loss). |

## 19. Related Documents

- `PROJECT_PLAN.md` §"Sprint 34" · `KNOWN_UNKNOWNS.md` · `BASELINE_METRICS.md` · `MINE_DUAL_SUBSYSTEM_DESIGN.md` · `SARF_EMIT_MODE_DESIGN.md` · `FAWLEY_CORRECTION_FORCING_DESIGN.md` · `BOUND_TRANSFER_SIGN_DESIGN.md` · `CAMCGE_ROCKET_PLAN.md` · `PHASE_0_ACCEPTANCE_GATES.md` · `REPLAN_RISK_ASSESSMENT.md` · `TOOLING_AND_BACKLOG_ANALYSIS.md` · `prompts/PLAN_PROMPTS.md`

---

**Document Created:** 2026-07-20
**Owner:** Sprint 34 Planning Team
**Status:** Sprint 34 is **GO for Day 0** — all 11 prep tasks complete; the schedule front-loads the deep + fresh tracks (P1 mine + P4 bound-transfer + P3 fawley, with the P2 sarf O(active) probe Day 0) so every deep-track REPLAN surfaces by the Day-5 checkpoint, with the honest modal-flat-KPI projection binding (P1 High-prior on an unproven H_dual against a reframed cold-MS-1 gate; P3's +Solve a forcing hand-off; P4's +Solve contingent on agreste; P5 camcge Epic-5-deferred + rocket → Sprint-35; the P6 failure-cohort the designated best-remaining-shot).
