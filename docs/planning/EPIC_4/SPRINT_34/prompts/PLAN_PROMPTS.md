# Sprint 34 Per-Day Execution Prompts

One self-contained prompt per day (Day 0 + Days 1–13). Each is derived from `../PLAN.md` and the Sprint-34 prep outputs. Run one per day.

## How to Use

Paste the day's prompt as the task. Each prompt names its objective, branch, Phase-0 gate, quality gate, and the PR + wait-for-review step. Branches: `planning/sprint34-dayN-<slug>`.

## Cross-Cutting Rules (every day)

- **PR24 (Day-0 traced fix surface):** the banked `file:line` is a *hypothesis* — re-confirm it with `kkt_residual.py` / a `/tmp` control **before** any `src/` change. **For P1 mine the cross-term premise is REFUTED** (S32 `N`-derivation + S33 Task-3: the emitted `stat_x` cross-term is algebraically correct) **and H1 keying is value-invariant** (S33 Day-2) — the fix is **H_dual** (a structural complementarity-pairing change via `head_domain_offsets`), NOT a term/sign change and NOT a keying tweak. **The objective-gradient sign flip is BANNED for P5** (control-refuted 4× S30–S31).
- **Assert `modelstat` before reading any objective; `x.up=inf` is BANNED** (mine — the S31 Day-2 measurement error). **Check the dual side (S30 camcge lesson):** any transform that redefines a dual must reach the target on the KKT *dual* (a `/tmp` prototype to MS-1), not just the primal. **P1's gate is the COLD MCP reaching MS-1 @ 17500** — not the warm residual `N→0` (keying/pairing-invariant, so un-passable by construction).
- **PR25 (projection discipline):** track genuine-floor vs methodology separately; re-baseline after any pipeline-methodology change. The genuine-floor ramp is *conditional* (Task 9) — only emit-changing / recovered tracks advance it; **P5 = 0 floor, P4 = 0 floor directly**. Anchor **75**.
- **Emit-touching PRs:** include the regenerated `.gms` diff (PR14), pass the golden-staleness check (PR26) + the presolve-divergence detector, and the **`--resolve-changed --since-commit 750803b2` checkpoint** (the **Day-0 code anchor**, the S33 close; the old byte-anchor `4cbf8bff` is **historical** — the S33 P6 sample fix moved the DB off it).
- **Quality gate (any `*.py` change):** `make typecheck && make format && make lint && make test` must pass before commit. Docs-only days skip it.
- **REPLAN honesty:** each REPLAN-gated day has a firm part that lands regardless; file the Sprint-35 carryforward on REPLAN (reallocation order P6 → P7 → the rocket forcing tail).

---

## Day 0 Prompt — Kickoff + Day-0 Traces + Control Probes (~6 h)

Confirm Day-0 = Sprint 33 close (`../BASELINE_METRICS.md`: Solve 108 / Match 93 / genuine floor 75 / model_infeasible 7 / path_syntax_error 7 / Translate 135 / all-219 Match 96 / Tests ~5,034). Verify `git diff 750803b2..HEAD -- src/ scripts/` is empty before skipping the retest; if non-empty, run a fresh retest. Run the **Day-0 traces (PR24)** and re-confirm each Phase-0 gate's Day-0 fingerprint (`../PHASE_0_ACCEPTANCE_GATES.md` §1): mine (`kkt_residual.py` → CASE_B `stat_x(3,1,1)` rel 2.37, dual CONSISTENT — **and** the Task-3 findings: the cross-term is *algebraically correct*, H1 re-keying **value-invariant**; residual = the 22-row `c`-boundary + `d\c`-ring), sarf (the three enumeration sites + the 369K-vs-398-active count + the live > 116s blow-up), fawley (CASE_B `stat_bq(res-arab-l,fuel-oil)` 0.973 raw 473; the constraint-index-diagonal control 473 → 18.468 — the 18.468 is the P4 cc-dist cell), bound-transfer (the min-convention gates at `src/emit/emit_gams.py:1590`/`:1603`; `bq.m=-18.468` + the mine 3 upper-bound `x.m>0` rows), camcge (CASE_B `stat_mps` cleared by S32 step 1; residual MS-4 Walras), rocket (Case-c clean at the NLP point — boundary `stat_ht`/`stat_step`). Then run the **control probes** (all before any `src/`): (1) **P2 sarf O(active) probe (run FIRST — the cheapest, earliest verdict):** confirm the sparsified `stat_task$taskposs` enumerates 398 active, not 369,024 — retires the sprint's dominant translate-timeout risk Day 0; (2) **P1 H_dual `/tmp` structural prototype (highest prior):** anchor `comp_pr`/`lam_pr`'s complementarity to the head-side variable at the head label `(k,l+1,i,j)` via the unused `head_domain_offsets` IR on a scratch `/tmp/mine_mcp_prototype.gms` + `/tmp/mine_mcp_presolve_prototype.gms` (**not** the committed golden), run from the **repo root** → **the COLD MCP reaches MS-1 @ 17500** (`modelstat=1` asserted; `x.up=inf` BANNED), the 22 boundary rows closing in the cold solution + interior rows unperturbed — NOT `N→0` at the warm point (keying-invariant); (3) **P3 fawley localize-by-column `/tmp`:** re-confirm `max|stat_bq|` 473 → 18.468 and confirm the residual localizes to the P4 cc-dist cell (`max|stat_bq| → 0` needs P3 + P4 together); (4) **P4 bound-transfer `/tmp` cell:** confirm `= abs(var.m)` closes the fawley cc-dist warm cell + the mine 3 upper-bound rows; (5) **P5 camcge/rocket clean-at-NLP re-confirm.** Restate the PR25 tally (genuine 75; the → +1 conversion map: P1 H_dual cold-match; P3's genuine correction *if forcing lands*; P6 ganges/gangesx cold-match — the firmest, the S33 precedent). Docs/trace-only (no `src/`). **No PR** (or a docs-only trace-notes PR).

## Day 1 Prompt — Priority 1: mine H_dual head-offset dual reconciliation (start) (~7 h)

Branch `planning/sprint34-day1-mine-hdual`. **The cross-term is already correct (S32/S33) and H1 keying is value-invariant (S33 Day-2) — do NOT change terms/signs and do NOT merely re-key the multiplier.** Implement **H_dual**: anchor the head-placed precedence dual `pr.m(k,l+1,i,j)`'s *complementarity* to the head-side variable via the `head_domain_offsets` IR carrier (`src/ir/symbols.py` field + `src/ir/parser.py` populate, both on `main` from S31) wired into `_try_build_param_offset_crossterm` (`src/kkt/stationarity.py:5712`) + the `_emit_nlp_presolve` transfer (`src/emit/emit_gams.py`) — see `../MINE_DUAL_SUBSYSTEM_DESIGN.md` §5. Gate the reconciliation to the head-offset-coupled case so the non-mine param-offset cohort (srpchase) stays byte-stable. **Phase-0 gate:** `../PHASE_0_ACCEPTANCE_GATES.md` §1 P1 (the Day-0 H_dual `/tmp` cold-MS-1 prototype must have passed before src). Quality gate + emit-touching PR (WIP if incomplete) + wait for review.

## Day 2 Prompt — Priority 1: mine cold-MS-1 verification (~7 h)

Re-run `kkt_residual.py data/gamslib/raw/mine.gms` after the H_dual reconciliation, then solve the **cold** MCP → **MS-1 @ 17500** (`modelstat=1` asserted; `x.up=inf` BANNED), the 22 boundary rows closing *in the cold solution* + interior rows unperturbed (+1 Solve; +1 genuine floor if it cold-matches). **NB — the gate is the cold solve, NOT warm `N→0`** (keying/pairing-invariant). Confirm the S31 head-offset foundation guard tests stay green and the non-mine presolve goldens byte-stable (`--resolve-changed --since-commit 750803b2`). Quality gate + emit-touching PR + wait for review.

## Day 3 Prompt — Priority 1: mine close-or-REPLAN (~6 h)

Complete the mine H_dual reconciliation. **PROCEED** (mine `model_infeasible → model_optimal`, +1 Solve; +1 genuine floor if cold-match) if the **cold** MCP reaches MS-1 @ 17500 with no interior perturbation and no srpchase regression. **REPLAN mine → a deeper head-offset dual architecture** (prior **High** — the banked premise was twice-refuted; the boundary is a genuine dual-degeneracy the emit cannot deterministically reconcile, or a PATH-consultation question) if H_dual cannot drive the cold MCP to MS-1 @ 17500 without perturbing an interior row or regressing srpchase — file the Sprint-35 carryforward; the cross-term-correct + H1-value-invariant findings + `../MINE_DUAL_SUBSYSTEM_DESIGN.md` + the S31 IR foundation land regardless; freed ~14–18 h → P6/P7 (Task 9). **Phase-0 gate:** `../PHASE_0_ACCEPTANCE_GATES.md` §1 P1. Quality gate + emit-touching PR + wait for review. *(P1 total ~20 h across Days 1–3.)*

## Day 4 Prompt — Priority 4: max-convention bound-transfer-sign + agreste +Solve survey (~8 h)

Branch `planning/sprint34-day4-bound-transfer`. Implement the sign-robust `piL_*/piU_*` transfer — `= abs(var.m)` at the **active bound**, **Option B sense-aware** (`model_ir.objective.sense == ObjSense.MAX`; precedent `src/ad/gradient.py:300`), so MINIMIZE stays byte-identical and only MAXIMIZE goldens change — at the min-convention gates `src/emit/emit_gams.py:1590` (`piL`: `…and var.m > 0`) + `:1603` (`piU`: `…and var.m < 0`) (`../BOUND_TRANSFER_SIGN_DESIGN.md`). The transfer fires **only** at active bounds (no over-transfer on interior/presolve-match models). Then the **+Solve survey (primarily agreste):** does the sign-robust transfer close the warm residual AND reach MS-1 (warm-residual-driven → +Solve) vs stay MS-5 (structural)? **Honest finding (Task 6):** the MAXIMIZE `model_infeasible` cohort is otherwise-attributed (fawley H-b, mine `x.m=0`, camcge Epic-5, rocket Case-c), so the realistic +Solve target reduces to agreste — P4's firm value is the general warm-start-correctness fix. **Documented-finding exit (a-priori-likely, NOT a correctness REPLAN):** no candidate warm-residual-driven → ship as a general-correctness fix with no +Solve; over-transfer / MAXIMIZE-presolve regression (`--resolve-changed --since-commit 750803b2` NO-GO) → re-scope. **Phase-0 gate:** `../PHASE_0_ACCEPTANCE_GATES.md` §1 P4. Quality gate + emit-touching PR + wait for review.

## Day 5 Prompt — Priority 3: fawley constraint-index-diagonal correction + Checkpoint 1 (~8 h)

Branch `planning/sprint34-day5-fawley-secondindex`. Extend the general indexed cross-term `sameas`-guard path — `src/kkt/stationarity.py` (`_build_sameas_guard`/`_get_or_create_fresh_alias` in `_add_indexed_jacobian_terms:5861`) — so **every** qsb/pbal second-index `cfq` gets the `$(sameas(cfq__, cf))` restriction the mbal term already has, covering the 2-D cross-terms `bq(c,cf)` (473 → 18.468) (`../FAWLEY_CORRECTION_FORCING_DESIGN.md`). **Do NOT touch the 1-D polygon core** (`_var_at_two_indices_complement:7291` — polygon/ps2) and **no mbal-term change**. **The +Solve is a P5 forcing hand-off (H-b confirmed):** the correction ships for correctness (a cold-emit change), and the +1 genuine floor is **contingent on forcing** (fawley does not cold-match under H-b — `max|stat_bq| → 0` needs P3 + P4 together and *still* solves MS-5 @ 4399.557). **REPLAN only** if the generalization leaks onto the mbal / variable-index-diagonal shape or regresses the 1-D core → re-scope; freed ~6–12 h → P6/P7. Then **Checkpoint 1:** `--resolve-changed --since-commit 750803b2` re-solve of the changed-golden set (bucket-diff vs the committed DB) + golden-staleness + the PR25 re-baseline. **NO-GO** if any changed-golden model moved backward → investigate before proceeding. *(All front-loaded gates have now fired: P1 cold-MS-1 Day 3, P4 +Solve survey Day 4, P3 correctness Day 5; P2's timeout risk cleared at the Day-0 probe.)* **Phase-0 gate:** `../PHASE_0_ACCEPTANCE_GATES.md` §1 P3. Quality gate + emit-touching PR + wait for review.

## Day 6 Prompt — Priority 2: sarf three-site symbolic emit (start, REPLAN-gated) (~6 h)

Branch `planning/sprint34-day6-sarf-symbolic`. Begin the **atomic three-site O(active) symbolic re-emit** (`../SARF_EMIT_MODE_DESIGN.md`). Eliminate the 369,024-column materialization at all three sites (a partial = an inconsistent MCP): S1 the `acost3` body-differentiation (`src/ad/constraint_jacobian.py`), S2 the variable-column enumeration (`src/ad/index_mapping.py`), S3 the variable stationarity (`src/kkt/stationarity.py`) — replaced by one symbolic guarded `stat_task(g,t,m,n)$taskposs(g,t)` (the banked 7-term derivation) + `task.fx(g,t,m,n)$(not (taskposs(g,t) and tech(g,m,n))) = 0`. **No set-name-literal multiplier indices** (the Sprint-26 `nu_slack("srn")` failure, commit `243fe578`; scan two greps — `grep -E 'nu_[[:alnum:]_]+\("' sarf_mcp.gms` and `grep -E 'lam_[[:alnum:]_]+\("' sarf_mcp.gms`, both empty). **Phase-0 gate:** `../PHASE_0_ACCEPTANCE_GATES.md` §1 P2. Quality gate + emit-touching PR (WIP) + wait for review.

## Day 7 Prompt — Priority 2: sarf tractability gate (~6 h)

The re-emit must be **O(active = 398), not O(369,024)** — time `sarf` translate (target **seconds**, cf. srpchase's 1-D analogue ~2.9s; the failure is > 116s). **PROCEED** (sarf `translate_failure → translate`, +1 Translate) if sub-budget. **REPLAN** (a documented parametric-emit re-scoping — a 4th enumeration site / builder-pipeline materialization; freed ~9–20 h → P6/P7) if it re-triggers the timeout. *(The Day-0 O(active) probe already retired the dominant risk; this is the full timing confirmation.)* Verify the emitted `stat_task` term-for-term against the banked 7-term derivation. Quality gate + emit-touching PR + wait for review.

## Day 8 Prompt — Priority 2: sarf atomic land (~6 h)

Land the three-site fix **atomically** (S1 + S2 + S3 + the `task.fx` fixing in one PR — a re-emit without cross-terms is an inconsistent MCP). Confirm the emitted `stat_task$taskposs` is the symbolic guarded form over 398 active instances, no set-name literals. Quality gate + emit-touching PR + wait for review.

## Day 9 Prompt — Priority 2: sarf close + golden (~6 h)

Close sarf: golden byte-stable (sarf's *new* `sarf_mcp.gms` golden — caught by the golden-staleness gate, since `--resolve-changed` diffs *existing* goldens) + deterministic ×3 `PYTHONHASHSEED`; `--resolve-changed --since-commit 750803b2` GO (sarf the only changed golden). If the P2 tractability gate REPLAN'd (Day 7), file the Sprint-35 re-scoping instead and reallocate the freed budget → P6/P7. Quality gate + emit-touching PR + wait for review. *(P2 total ~24 h across Days 6–9.)*

## Day 10 Prompt — Priority 5 camcge Epic-5 gate + rocket Sprint-35 submission + Checkpoint 2 (~10 h)

Branch `planning/sprint34-day10-camcge-rocket`. **P5 camcge Epic-5 `/tmp` gate (confirm the deferral — `../CAMCGE_ROCKET_PLAN.md`):** run the `/tmp` prototype of the full dual-consistent redefinition (keep every market-clearing row + the consumption-weighted numéraire + redefine the redundant market's dual via Walras' law), checking the KKT **dual** side. **Expected: MS-4** → Epic-5-deferred (camcge stays `model_infeasible`; the numéraire recipe + the S1∧S2∧S3 detector — which must flag **only** camcge, S3 = cold-MCP-singular-at-iter-0 the false-positive guard — are the Epic-5 hand-off). **Promote to +1 Solve only if** it unexpectedly reaches MS-1 at omega 191.7346 (step 1 landed S32, PR #1553). **P5 rocket (`../CAMCGE_ROCKET_PLAN.md` — no emit fix, sign flip BANNED):** re-confirm rocket's residual is clean at the NLP point (the boundary signature, dual CONSISTENT — a *forcing* problem, not an emit bug); **submit** the FINALIZED PATH-consultation input to the **Sprint-35** consultation; the `--force` survey is exhausted (all MS-5). **0 genuine floor.** **Checkpoint 2:** `--resolve-changed --since-commit 750803b2` re-solve + golden-staleness + the PR25 tally. PR (P5 docs unless the camcge `/tmp` unexpectedly crosses to emit) + wait for review.

## Day 11 Prompt — Priority 6: failure-cohort re-triage + REPLAN-slack (~11 h)

Branch `planning/sprint34-day11-p6-cohort`. **P6 ganges/gangesx `$141/$145/$149` cohort (`../TOOLING_AND_BACKLOG_ANALYSIS.md` §3 — the designated best-remaining-shot):** the two share a single, live-confirmed root — the NaN-sanitization emit pass emits a self-referential guard `param(i)$(NOT (param(i) > -inf and param(i) < inf)) = 0` over parameters (adst/aex/aid/…) whose source value-assignment depends on a variable level `dst.l` (`$141` = "Symbol declared but no values assigned"; both: `$141`×15 / `$145`×3 / `$149`×9). **A single fix may recover both**, distinct from sample's `$140` (the S33 P6 fix — a no-op here). Each `--resolve-changed --since-commit 750803b2`-gated + a golden-staleness check on the new goldens. **P6 agreste scope-verify (§2):** inspect the agreste source for the double-`solve` scope (two `solve … maximizing yfarm using lp`, `agreste.gms:294`/`:298`) BEFORE treating the CASE_B `stat_sales` as an emit bug (agreste is P4-entangled). If the single-solve scope holds CASE_B → a +Solve candidate (`--resolve-changed`-gated); if a driver artifact → bank. The cohort is **multi-root** (verify per-model). **REPLAN-slack:** whatever the P1/P2/P3/P4 REPLANs freed re-allocates here first (Task 9 order: P6 → P7 → the rocket tail). Deliverable: ≥ 1 model recovered OR the cohort re-triaged with banked diagnoses. Quality gate (if a candidate lands emit) + PR + wait for review.

## Day 12 Prompt — Priority 7: infrastructure + REPLAN-slack (~8 h)

Branch `planning/sprint34-day12-p7-infra`. **P7 property fixtures (each fail-before/pass-after, only once its track's fix landed):** **shape12** (head-offset dual — guards P1), **shape13** (sarf symbolic `stat_task` — guards P2), a **fawley 2-D second-index fixture** (guards P3, distinct from the 1-D shape10/11), plus an **optional P4 MAXIMIZE bound-transfer fixture** (added only if P4's correctness fix landed) → `tests/integration/emit/test_ad_crossterm_shapes.py` (+ the `test_sample_pruned_var_l_init.py` raw-emit + skip-if-absent pattern for any recovered P6 model). **Genuine-floor tracking:** recompute the PR25 genuine-floor tracking (anchor **75**); refresh the `--resolve-changed` checkpoint targets for the newly-touched emit sites. **Epic-4 `SUMMARY.md` row-34 continuation:** (1) reconcile the theme cell — the pre-insertion row holds the Quality/PATH-consultation theme (now Sprint 35); Sprint 34's is "Sprint 33 REPLAN'd/deferred carryforwards"; (2) fill the cells in the rows-28–33 format {Theme / Headline KPIs / Firm landing(s) / REPLAN'd → carryforward}; add row 35. **REPLAN-slack:** absorb residual freed budget per the Task-9 reallocation order. Quality gate (tests/ changed) + PR + wait for review.

## Day 13 Prompt — Final Retest + Closeout (~8 h)

Branch `planning/sprint34-day13-close`. **Full pipeline retest** under ≥ 3 `PYTHONHASHSEED` values (PR12); recompute the DB (machine-portable paths) + the Sprint 33 → 34 metrics comparison; **PR25 genuine-vs-methodology re-baseline** recomputed (genuine floor anchor 75). **Closeout:** `SPRINT_LOG.md` final entry + top-table + per-priority summary; `SPRINT_RETROSPECTIVE.md` authored; the Sprint-35 carryforwards filed (mine if REPLAN'd → deeper head-offset dual architecture; sarf if REPLAN'd → re-scoping; fawley +Solve → the P5 forcing tail if not landed; the camcge numéraire → Epic 5; the rocket PATH-consultation input → the Sprint-35 consultation; cesam/lnts Case-c; any un-landed P6 candidate). Fill the SUMMARY row-34 cells. Docs/DB PR + wait for review.

---

**Document Created:** 2026-07-20 · **Owner:** Sprint 34 Planning Team · derived from `../PLAN.md` + the Tasks 1–10 prep outputs.
