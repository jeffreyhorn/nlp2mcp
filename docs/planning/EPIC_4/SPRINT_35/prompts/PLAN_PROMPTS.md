# Sprint 35 Per-Day Execution Prompts

**Companion to:** `../PLAN.md` (the 14-day schedule) · **Owner:** Sprint 35 Planning Team · **Created:** 2026-07-24

## How to Use

Paste the day's prompt as the task. Each prompt names its objective, branch, Phase-0 gate, quality gate, and the PR + wait-for-review step. Branches: `planning/sprint35-dayN-<slug>`, created from `main`, PRs target `main`. Per-day workflow: branch → work → quality gate ONLY if `*.py` changed → commit → push → PR → user merges → "checkout main and pull"; docs/DB/golden-only PRs skip the gate.

## Cross-Cutting Rules (every day)

- **PR24 (Day-0 traced fix surface):** the banked `file:line` is a *hypothesis* (wrong ~half the time across S27–S34) — re-confirm with `kkt_residual.py` / a `/tmp` control **before** any `src/` change. **For P4 `$149` the surface is `src/ad/derivative_rules.py:_diff_prod:3276`, NOT `stationarity.py:_add_indexed_jacobian_terms` (refuted).** For P1 mine the whole keying/pairing space is **value-invariant** (REPLAN'd in prep — no in-sprint build). **The objective-gradient sign flip is BANNED (Case-c, refuted 4×).**
- **Assert `modelstat` before reading any objective; `x.up=inf` is BANNED** (mine — the S31 measurement error). **Check the dual side** (the S30 camcge lesson) for any dual-redefining transform.
- **The failure cohort is multi-root (the S34 Day-11 lesson):** verify **per model**, never infer one model's roots from another's; **no model recovers until its whole root-set clears.** Compile-clean-but-not-solving is `path_syntax_error → model_infeasible`, a *different* bucket — report it as such, not a recovery.
- **PR25 (projection discipline):** track genuine-floor vs methodology separately; a warm-start/presolve-only match is **methodology = 0 floor**. Anchor **75**. **The projection is BIMODAL on P4:** Solve 108 or 110; floor 75 or 77 (cold-match-contingent); Translate 135; **≥ 112 is REFUTED — do not promise it.**
- **Emit-touching PRs:** include the regenerated `.gms` diff (PR14), pass the golden-staleness check (PR26 — **scoped** `--models` for P4, never the unscoped `make regen-goldens`) + the presolve-divergence detector, determinism ×3 `{0,1,42}` (PR12), and the **`--resolve-changed --since-commit 78ceaead`** checkpoint (the **Day-0 code anchor**, the S34 close; the old anchor `750803b2` is **historical** — `src/` changed at S34 Day 4 via P4).
- **Quality gate (any `*.py` change):** `make typecheck && make lint && make format && make test` must pass before commit. Docs-only days skip it.
- **"No bucket → no `src/`"** with the S34-P4 exception (fast, regenerable goldens + `--resolve-changed` GO). **P4 is expected to invoke it** — steps 1–2 (`$141`/`$145`) bank if `$149` REPLANs.
- **REPLAN honesty:** each REPLAN-gated day has a firm part that lands regardless; file the Sprint-36 carryforward on REPLAN (reallocation order P6 → P7 → the Sprint-36 bundle).

---

## Day 0 Prompt — Kickoff + Day-0 Traces + Control Probes (~6 h)

Confirm Day-0 = Sprint 34 close (`../BASELINE_METRICS.md`: Solve 108 / Match 93 / genuine floor 75 / model_infeasible 7 / path_syntax_error 7 / Translate 135 / Parse 142 / all-219 Match 96). Verify `git diff 78ceaead..HEAD -- src/ scripts/` is empty before skipping the retest; if non-empty, run a fresh retest. Run the **Day-0 traces (PR24)** and re-confirm each Phase-0 gate's Day-0 fingerprint (`../PHASE_0_ACCEPTANCE_GATES.md` §1), **P4 FIRST (the sole live gate):** ganges/gangesx per-model compile counts (`$141×15` corpus, `$145×3`, `$149×9` on ganges); that `$141` at `src/emit/original_symbols.py:152` still removes all 15 (Task 5 scratch re-emit); the Task-4 hand-derived `stat_pc` cross-term `prod(j,(pc(j)/pc00(j))**ac(j,r)) * ac(i,r)/pc(i)` and the 18-model prod-in-stationarity regression set (lmp2 most sensitive). Then the pre-refuted/deferred gates for the record: **P1 mine** boundary is `x.m=0`-degenerate (CASE_B, dual CONSISTENT — no candidate reaches cold-MS-1 @ 17500, no `/tmp` control warranted; exit → Sprint-36); **P2 sarf** O(active=398)-not-369,024 + measured > 303 s (DEFER'd); **P3 fawley** CASE_B `stat_bq` raw 473 → 18.468 + H-b (MS-5 @ 4399.557, the emit-correct `stat_trans(tr-2)` residual the harness max); **P5 camcge** detector fires only on camcge, expected MS-4; **rocket** clean at the NLP point (CASE_C_OBJDEF, dual CONSISTENT 1.53e-10). Restate the PR25 tally (genuine 75; the → +2 conversion map is entirely P4-contingent and specifically on a **cold** match). Assert the GO/NO-GO for Day 1 (`../PLAN.md` §19 — GO). `modelstat` asserted before every objective read; `x.up=inf` BANNED; the Case-c sign flip BANNED. Docs/trace-only (no `src/`). **No PR** (or a docs-only trace-notes PR).

## Day 1 Prompt — Priority 4: ganges/gangesx `$141` + `$145` banked-root re-apply (~7 h)

Branch `planning/sprint35-day1-p4-banked-roots`. Re-apply the banked-and-verified `$141` fix (`_param_assignment_references_varref_attr` skip in `emit_post_assignment_na_cleanup`, `src/emit/original_symbols.py:152`, mirroring `_param_assignment_has_division:137`) + the `$145` universal-set (`*`-domain) skip in the same cleanup loop (`../GANGES_RECOVERY_DESIGN.md`, Task 5). Confirm the re-emit removes all 15 `$141` + the `$145×3`. Regenerate the ~9 `.l`-calibration collateral goldens with the **scoped** `scripts/sprint_audit/check_golden_staleness.py --models …` `--fix` (`../TOOLING_AND_BACKLOG_ANALYSIS.md`, Task 3; **never** the unscoped `make regen-goldens` — its contention caused the S34 soft-timeout) + determinism ×3 `{0,1,42}`. **⚠ No bucket moves yet** — the `$141`+`$145` re-emit leaves `$149×9` (Task 5, proven); this is the **expected** mid-sequence flat state, and steps 1–2 **do not ship on their own** ("no bucket → no `src/`"). Phase-0 gate: `../PHASE_0_ACCEPTANCE_GATES.md` §1 P4 (roots 1–2). Quality gate (Python touched). Commit + PR as **WIP not yet shipped** (or hold the branch for the all-three-roots landing on Day 3–4). Then wait for reviewer comments.

## Day 2 Prompt — Priority 4: `$149` `/tmp` control BEFORE `src/` (the sole live REPLAN gate) (~8 h)

Branch `planning/sprint35-day2-p4-149-control`. **PR27 `/tmp` control BEFORE any `src/` change** (`../GANGES_149_PRODUCT_RULE_ANALYSIS.md` Task 4 + `../GANGES_RECOVERY_DESIGN.md` Task 5): prototype the AD-layer product-rule fix at `src/ad/derivative_rules.py:_diff_prod:3276` + the emit-alias contract (**NOT** `stationarity.py:_add_indexed_jacobian_terms` — refuted). The control must (a) reproduce Task 4's hand-derived `stat_pc` cross-term `prod(j,(pc(j)/pc00(j))**ac(j,r)) * ac(i,r)/pc(i)` (`i` controlled, no free `j`), (b) drive ganges's 9 `$149` → 0, and (c) leave the **18-model prod-in-stationarity regression set byte-identical** (lmp2 the most sensitive — the name-match case the collapsed `_diff_prod` branch relies on). `modelstat` asserted; `x.up=inf` BANNED. **This is the sprint's only live REPLAN gate. REPLAN exit:** if `_diff_prod` cannot be made surgical against the 18-model set → **bank all three roots** (steps 1–2 alone = 0 bucket + golden churn, the S34-banked outcome — no `src/` ships) and reallocate P4's 14–20 h to P6/P7 (Day 5 records it). Phase-0 gate: `../PHASE_0_ACCEPTANCE_GATES.md` §1 P4 (`$149`). `/tmp`-only (no `src/`). Docs/control-notes PR (or no PR). Then wait for reviewer comments.

## Day 3 Prompt — Priority 4: `$149` `src/` land + per-model residual-count (~8 h)

Branch `planning/sprint35-day3-p4-149-land`. If the Day-2 `/tmp` control passed: apply the `_diff_prod` correction to `src/`, then for **ganges and gangesx independently** emit → compile (`gams a=c`) → **count residual `$NNN` by code (assert 0)** — never infer one model from the other (the S34 Day-11 lesson: a fourth per-model root can surface at the compile step). Regenerate goldens (scoped `--fix`) + determinism ×3. If the Day-2 control REPLAN'd, do **not** land `$149` — skip to the Day-5 bank-and-reallocate. Phase-0 gate: `../PHASE_0_ACCEPTANCE_GATES.md` §1 P4 (the per-model protocol, encoded so it can't be skipped). Quality gate (Python touched). Commit + PR (emit-touching, or fold into the Day-4 all-three-roots landing). Then wait for reviewer comments.

## Day 4 Prompt — Priority 4: per-model solve + match verification (~8 h)

Branch `planning/sprint35-day4-p4-verify`. For **ganges and gangesx independently**: translate → solve **cold AND presolve** (`modelstat` asserted) → bucket → match classification. **Answer Unknown 4.4** (DESIGN-SPECIFIED — the recovery verdict, the one aspect not executed in prep): does each model actually solve-and-match, and **cold or presolve-only**? Record the genuine-floor consequence honestly — **cold-match = +1 floor each; presolve-only = methodology = 0 floor** (Match still +1 each). This is the all-three-roots landing (the "no bucket → no `src/`" discriminator: does the full landing move ganges/gangesx out of `path_syntax_error`? If yes → ship, goldens regenerable + `--resolve-changed` GO; if no → bank all three). Phase-0 gate: `../PHASE_0_ACCEPTANCE_GATES.md` §1 P4 (per-model). Quality gate (Python touched). Commit + PR (emit-touching). Then wait for reviewer comments.

## Day 5 Prompt — Priority 4: close-or-REPLAN + Checkpoint 1 (~7 h)

Branch `planning/sprint35-day5-p4-close-checkpoint`. **Checkpoint 1:** `scripts/gamslib/run_full_test.py --resolve-changed --since-commit 78ceaead` re-solve of the changed-golden set (bucket-diff vs the committed DB) + `check_golden_staleness.py` (PR26) + `check_presolve_divergence.py` + the PR25 re-baseline. **NO-GO** if any *unchanged* golden moved backward (`match→mismatch`, `model_optimal→model_infeasible`, presolve-match→abort). Record the P4 verdict (PROCEED with +2 / +1 / bimodal-flat, or REPLAN'd). **Freed-budget reallocation:** if P4 REPLAN'd, its 14–20 h joins P6/P7 (already the plan); if it landed, record the bucket move + the cold-vs-presolve floor verdict. The sprint's only live REPLAN gate has now fired. Phase-0 gate: `../PHASE_0_ACCEPTANCE_GATES.md` §2 (cross-cutting). REPLAN exit explicit. Quality gate if Python touched. Commit + PR (emit-touching, or docs-only if banked). Then wait for reviewer comments.

## Day 6 Prompt — Priority 6: residual failure-cohort re-triage, part 1 (~8 h)

Branch `planning/sprint35-day6-p6-cohort-1`. Work Task 4's per-model catalog (`../GANGES_149_PRODUCT_RULE_ANALYSIS.md`) — the residual `path_syntax_error` cohort that `$149` does **not** unblock (only ganges/gangesx are product-rule beneficiaries): **turkey `$161`** (the dotted-tuple set — a-priori hard) + **dinam/indus** (`$140`+`$149`). **Multi-root discipline (Unknown 6.2):** verify per-model, never infer; **no model recovers until its whole root-set clears.** Each candidate passes `--resolve-changed --since-commit 78ceaead` GO before landing; `modelstat` asserted. Bucket at stake: up to +1 Solve/Match if a whole root-set clears (a-priori hard). Quality gate if Python touched. Commit + PR. Then wait for reviewer comments.

## Day 7 Prompt — Priority 6: residual failure-cohort re-triage, part 2 (~8 h)

Branch `planning/sprint35-day7-p6-cohort-2`. Continue the P6 catalog: **turkpow/clearlak** (`$149`+`$171`) + any recovery landing from Day 6. Same multi-root discipline + `--resolve-changed` GO gate + `modelstat`. Land any whole-root-set recovery (goldens regenerable, `--resolve-changed` GO); a flat P6 is acceptable — P6 is the *second* bucket source, not the headline. Quality gate if Python touched. Commit + PR. Then wait for reviewer comments.

## Day 8 Prompt — Priority 5: camcge Epic-5 gate + Sprint-36 bundle (rocket + mine + fawley) (~6 h)

Branch `planning/sprint35-day8-p5-epic5-sprint36`. **camcge Epic-5 `/tmp` gate (expected MS-4):** prototype the full dual-consistent Walras redefinition (`../CAMCGE_ROCKET_PLAN.md`, Task 9) → MS-1 @ omega 191.7346 (dual side, `modelstat` asserted). Expected MS-4 (the price-pin variant + 3+ sprints of variants all stayed MS-4 at the correct primal) → the per-model-numéraire declaration is the documented **Epic-5** fallback; camcge is excluded from the in-sprint Solve target. **Sprint-36 hand-off bundle:** submit the FINALIZED rocket PATH-consultation input to **Sprint 36** with the renumbering fixes (11 "Sprint 33" refs + the "Sprint 35" refs → Sprint 36 — technical content current, only labels stale); bundle mine (the primal-degenerate-LP consultation question, P1's exit) + fawley (the H-b `--force` survey, P3's +Solve) into the same Sprint-36 package. **0 in-sprint bucket. No `src/`.** The Case-c sign flip stays BANNED. Docs-only PR. Then wait for reviewer comments.

## Day 9 Prompt — Priority 3: fawley constraint-index-diagonal correction (optional, correctness-only) (~8 h)

Branch `planning/sprint35-day9-p3-fawley` (**optional / low-priority — 0 bucket, H-b — skip if P4/P6 overran; must not have displaced them**). **PR27 `/tmp` control BEFORE `src/`** (`../FAWLEY_DIAGONAL_DESIGN.md`, Task 8): the constraint-index-diagonal `$(sameas(cfq__,cf))` guard on the `qsb`/`pbal` terms — the predicate in `_add_indexed_jacobian_terms` at `src/kkt/stationarity.py:5861` (distinct from #1049 `:7176` and the variable-index diagonal — a **hypothesis** to re-trace) — must drive **`max|stat_bq| → 0`** (scoped to `stat_bq`, NOT the harness global max, which retains the emit-correct `stat_trans(tr-2)` non-emit residual — Task 8's finding), not merely the 96% partial (473 → 18.468). **Leak-free:** no mbal-term change, the 1-D core + 2-D cohort (cesam2/camcge/ps2_f_s/ps2_s/ps3_s_gic/polygon) byte-identical; `--resolve-changed --since-commit 78ceaead` GO with fawley the only changed golden. **REPLAN exit (gate leak):** any mbal/cohort/1-D-core change or `max|stat_bq|` not reaching 0 → DEFER again (a dedicated effort + the 2-D-cohort harness); budget → P6/P7. **The +Solve is out of P3 scope** (H-b → the Sprint-36 `--force` survey; fawley stays `model_infeasible`). `modelstat` asserted. Quality gate (Python touched, if it lands). Commit + PR (emit-touching), or a docs-only DEFER note. Then wait for reviewer comments.

## Day 10 Prompt — Priority 7: infrastructure (fixtures + floor tracking) + Checkpoint 2 (~8 h)

Branch `planning/sprint35-day10-p7-infra-checkpoint`. P7 property fixtures, **each gated on its track's landing, fail-before/pass-after** (Unknown 7.1; the S33 `test_sample_pruned_var_l_init.py` skip-if-absent + S34 `test_p4_maximize_bound_transfer_sense_aware` patterns): a **ganges-recovery** raw-emit fixture **if P4 landed** (shape12 head-offset / shape13 sarf / fawley-2D are **not applicable** — P1/P2 ship no `src/`; P3 optional). Recompute the PR25 genuine-floor tracking against the anchor (**75 → 75 or 77**); refresh the `--resolve-changed` checkpoint targets; continue the Epic-4 `SUMMARY.md` row-35 groundwork (Unknown 7.3 — larger than a row fill). **Checkpoint 2:** `--resolve-changed --since-commit 78ceaead` re-solve + golden-staleness + the PR25 tally. Quality gate if Python touched. Commit + PR. Then wait for reviewer comments.

## Day 11 Prompt — REPLAN-slack + P6/P7 continuation (~8 h)

Branch `planning/sprint35-day11-slack`. Absorb any Day-5 REPLAN reallocation (P4 → P6/P7); continue the residual-cohort push or the P7 fixtures. With the retrospective-budget slack (~34–70 h real work vs the 168 h cap — P1/P2 spent in prep), this day is genuine buffer for a **thorough P4 + P6** push rather than spreading budget thin. Same `--resolve-changed` GO gate on any landing; `modelstat` asserted. Quality gate if Python touched. Commit + PR. Then wait for reviewer comments.

## Day 12 Prompt — Sprint-36 carryforward drafting + pre-retest staging (~8 h)

Branch `planning/sprint35-day12-carryforwards`. Draft `SPRINT_36_CARRYFORWARDS.md` (mine primal-degenerate-LP consultation + sarf dedicated symbolic-emit effort + fawley `--force` survey + rocket PATH consultation + any P4/P6 REPLAN); stage the Day-13 retest. Docs-only. Commit + PR. Then wait for reviewer comments.

## Day 13 Prompt — Final Retest + Closeout (~8 h)

Branch `planning/sprint35-day13-close`. Full retest; **determinism ×3 `PYTHONHASHSEED ∈ {0,1,42}`**; `--resolve-changed --since-commit 78ceaead` GO; the PR25 genuine-vs-methodology final tally; write `SPRINT_LOG.md` + `SPRINT_RETROSPECTIVE.md`. Close **honestly against the bimodal projection** (`../PLAN.md` §2): Solve **108 or 110**; Match **93 or 95**; genuine floor **75 or 77** (cold-match-contingent); Translate **135**; path_syntax_error **7 or 5**; **not ≥ 112** (a-priori refuted). State which mode landed (P4 PROCEED or bimodal-flat) + whether the floor moved (cold vs presolve). Quality gate if Python touched. Commit + PR (the close). Then wait for reviewer comments.

---

**Document Created:** 2026-07-24
**Owner:** Sprint 35 Planning Team
**Covers:** Sprint 35 Day 0 + Days 1–13 (front-loaded P4 per Task 11; Checkpoints Day 5 / Day 10; final retest Day 13)
