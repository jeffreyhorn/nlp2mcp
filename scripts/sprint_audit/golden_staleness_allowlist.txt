# Golden-staleness allowlist (Sprint 28 Priority 8).
# Models whose committed goldens are historical artifacts: the current emit no
# longer produces them (the model is out of scope), so a regen→diff is
# meaningless. One model id per line; '#' comments allowed.
#
# Multi-solve driver scripts (out of scope): emit aborts
#   "Model is a multi-solve driver script and is out of scope for nlp2mcp"
danwolfe
decomp
saras
# Discrete (MIP/MINLP) models (out of scope): emit aborts "... is discrete ..."
nemhaus
nonsharp
trnspwl
# Cross-environment byte non-determinism (Unknown 8.3): the emit is hash-seed-
# stable on macOS (local regen == committed golden, ×PYTHONHASHSEED) but the
# ubuntu CI emit differs by ~45 bytes — a platform-specific difference (most
# likely float formatting in this large dynamic model), NOT a stale golden or an
# emit change. Allowlisted so the gate does not flap; a future emit *change* on
# indus would still surface (as a drift WARNING on the allowlisted entry). A
# root-cause determinism investigation: #1461.
indus
